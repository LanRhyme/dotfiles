# 配置记忆

本文件作为 AI 代理的实时上下文缓冲区，记录 LanRhyme 系统配置的当前状态、近期结构决策和进行中的任务，以确保会话间的平滑交接

- **GitHub 个人主页状态卡片修复 (2026-08-20)**: `~/Projects/LanRhyme` (`LanRhyme/LanRhyme`) 修复失效卡片：① `GitHub Stats` 中的个人统计与常用语言卡片原 `sigma-five` 实例因 PAT Token 失效报错（Maximum retries exceeded），已切换至高可用实例 `github-readme-stats-eight-theta.vercel.app`；② 连续打卡统计卡片切换至 `github-readme-streak-stats-eight-theta.vercel.app`（原 Heroku 实例下线）；③ Android Studio 徽标跳转链接修正为 `https://developer.android.com/studio`；④ 详细指标折叠图路径由根相对路径 `/github-metrics.svg` 修正为相对路径 `./github-metrics.svg`，全部卡片均已通过 GitHub Camo 代理验证正常渲染
- **Android 手机环境隐藏与模块优化 (2026-08-20)**: 手机 Redmi K60 (`mondrian` / `ed3fdd92`，Android 16 / SDK 36，Kernel 5.10 Weil+ 内置 KernelSU Next)。完成环境隐匿、墓碑、救砖与自研模块优化：① `freEnhance` (`io.github.lanrhyme.freenhance`) 重构：彻底剥离虚拟小窗代码 (`FreeformHook`)，仅保留全局导航栏沉浸 (`ImmersiveHook`)、Niagara 图标触感弹簧动效 (`NiagaraHook`) 以及多任务卡片居中/460ms 优雅过渡/背景模糊 (`LauncherHook`)；② 修复 QQ 踢线问题（禁用 NTQQ 耗电优化并加白名单）；③ 采用 `HMA-OSS Zygisk 166` 替代闭源 HMA 解决 Android 16 下未激活问题；④ 安装 `Yet Another Bootloop Protector 8.138` (YABP) 自动救砖模块与 `Thanox 8.6` 后台管理
- **DeepSeek Harness 安装 (2026-08-16)**: `deepseek-harness-bin 0.1.0rc.6-2`（AUR，PKGBUILD 已审查：官方 npm registry `@deepseek-ai/dsh` 0.1.0-rc.6 + 官方 GitHub deepseek-ai/deepseek-harness，sha256 校验，prepare 仅 `npm install --global --allow-scripts=@deepseek-ai/dsh-subprocess-local,koffi,node-pty`，无危险操作）。上游 GitHub 0 releases 无预编译二进制，所有安装方式都要 npm 拉取数百 MB 依赖（含 @aws-sdk 全家桶，最终 337M）+ node-pty/koffi 原生编译。**安装要点**：paru 交互 sudo 无 TTY 失败（老问题），`npm_config_registry` 直连官方源 ~120KB/s（npmmirror 反而更慢 72KB/s，代理 7890 不可用），全量构建约 30-40 分钟（超时会被杀，需 nohup 后台 + 轮询 `~/tmp/deepseek-harness-build.log`）；装完 `pkexec pacman -U` 安装构建产物（**pkexec 不保留 cwd，必须绝对路径**）。已装：`/usr/bin/dsh`，`dsh --version` → 0.1.0-rc.6

## 当前配置状态

- **Noctalia MicYou 插件开发与开源发布 (2026-08-20)**: 开发、精简并独立开源发布 Noctalia 5 (Luau 脚本引擎) 桌面状态栏 MicYou 控制插件（ID: `lanrhyme/micyou`，`plugin_api = 3`，开源仓库：`https://github.com/LanRhyme/noctalia-plugin-micyou`，本地路径 `~/Projects/noctalia-plugin-micyou` 与 `~/.config/noctalia/plugins/micyou`）。全套组件：① `plugin.toml`（声明 `plugin_api = 3`、`[[service]]`、`[[panel]]`、`[[widget]]`、`[[shortcut]]` 与配置项）；② `micyou_service.luau`（纯后台无头守护服务，通过安全读取 `mode.lock` 与 `/proc/<pid>/cmdline` 监听状态，绝无多余进程唤起，同时在服务运行时流式解析实时输入电平 `[level]` 发布至 `audio_level` 状态流，控制 `micyou serve --mode <mode> --port <port>` 进程）；③ `widget.luau`（左键弹出附着式控制面板、右键快速启停、中键唤起 TUI、连接成功后麦克风图标转变为 21 阶 GPU 矢量多帧平滑渲染的实时音量环与中心圆点，静音时自适应警告色，移除多余文本）；④ `panel.luau`（精简高质感交互面板，支持 Wi-Fi/USB/Web 连接模式下拉切换、一键启停/重启、麦克风增益 Gain 0~30dB 滑块调节、AI 智能降噪 PureVox 开关、TUI 终端与桌面端启动入口）；⑤ `shortcut.luau`（控制中心快捷卡片）；⑥ `translations/`（中英文双语支持）。已清理全部冗余旧版 QML 文件，注册并编译部署独立 `micyou-cli` 至 `~/.local/bin/micyou` 与 `~/.local/bin/micyou-cli`，通过 chezmoi 全量同步
- **企业微信安装 (2026-08-17)**: `com.qq.weixin.work.deepin 5.0.0.6008~spark2-2`（Deepin Wine 版 WXWork 5.0.0.6008，星火商店维护）+ `spark-dwine-helper 5.8_5.3.14-1` + `deepin-wine10-stable 10.14deepin8-3` + `deepin-wine8-stable 8.16deepin25_1-1`。**AUR git clone 走 git 协议 TLS 失败**（FlClash 代理问题，同前），改 **tarball 直连**（`https://aur.archlinux.org/cgit/aur.git/snapshot/<pkg>.tar.gz` 可用）手动构建：`makepkg -f --nodeps`（跳过依赖检查，运行依赖最后统一由 pacman -U 解决）。PKGBUILD 全部审查安全：uniontech 官方 deb（wine8/wine10，sha256 校验，纯解包）、gitee spark-store release deb（helper，sha256 校验，解包+符号链接）、sdu spark-store 镜像 deb（wecom）。**修改**：wecom PKGBUILD 的 github 字体源（wqy-microhei.ttc，github 不可达）替换为 USTC Ubuntu `fonts-wqy-microhei_0.2.0-beta-3.1_all.deb`（sha256 58d0a29a...，包内路径 usr/share/fonts/truetype/wqy/wqy-microhei.ttc），package() 用 mkdir+bsdtar 提取（注意 bsdtar -C 需目录已存在）。**安装**：`sudo pacman -U --noconfirm` 4 个本地 pkg（用户交互终端输密码），依赖自动装（libgphoto2/libieee1284/net-snmp/python-pyqt5-sip/sane/python-pyqt5/zenity/p7zip/python-dbus）。启动：`/opt/apps/com.qq.weixin.work.deepin/files/run.sh`，desktop 已注册（Name=WXWork，Icon=com.qq.weixin.work.deepin），noctalia launcher 搜 WXWork 或企业微信。构建产物在 `~/tmp/wecom-review/`（含改后的 PKGBUILD 与备份），可复用升级。Wine 首次启动需初始化容器 `~/.deepinwine/Deepin-WXWork`（在 ~/.deepinwine/ 下，注意 MEMORY 主路径规则不适用于 wine 容器）。未主题化（wine 应用不走 morandi-gen）


- **ReveriePaint-native 多图层液化/变换交接 (2026-08-16)**: Android 绘画 App `~/Projects/ReveriePaint-native`（Kotlin + C++/Krita 核心，包名 com.reverie.paint，测试机 adb 序列号 ed3fdd92）。**ZCode 会话交接**：ZCode (Z.ai 3.7.7 AppImage，`~/.zcode` 数据 + `~/.zcode/cli/rollout/*.jsonl` 模型 IO 可读会话) 17:01 结束，任务为修复"多图层液化/变换没效果"。根因已定位并修复（2136c45：选中 1 个图层时旧规则回退当前层；9162d43：右滑多选斜率+左滑抽屉多选按钮；fa87420：点击已多选行保留多选集），**已安装到手机但用户未验证**。已清理未提交的调试 Log（clearLayerSelection 调用栈日志，其假设——液化时多选被清空——已被静态证伪，全项目唯一调用方是 LayerListView.onClick）。诊断日志保留：Kotlin `ReverieLq` tag toggle 日志 + C++ `ReverieCore` tag `liquifyBegin req=%d targets=%d`（RPC_LOG 走 logcat）。**实测反馈（17:10 logcat，用户选层1 cur=2）**：变换"UI 不表现"+移动/缩放位置偏差大 → 根因是 startTransformPreview/contentBounds 只处理当前层而提交作用于选中集，**已修（提交 b3734e5）**：Kotlin 传 editTargetLayers() 给 C++，预览（多事务提取+隐藏，m_previewTransactions）、手柄边界（联合 exactBounds）、提交（applyTransformLayers 联合中心）目标集完全一致；新增 JNI contentBoundsLayers/startTransformPreviewLayers。**用户二次反馈**：多选后只动选中集不动当前层 → 期望当前层也参与，**已修（提交 e965e3b）**：editTargetLayers 改为"选中集∪当前层"（Set 加法去重），当前层总是参与移动/变换/液化/预览/手柄边界。**用户已确认无问题**。多图层液化/变换/移动全部验证通过
- **OnlyOffice 安装 (2026-08-14)**: `onlyoffice-bin 9.4.0-1`（**CachyOS 官方仓库**，非 AUR，无需 PKGBUILD 审查，数字签名验证通过），命令 `sudo pacman -S cachyos/onlyoffice-bin`（sudo 需交互终端，pkexec 在 niri 下卡死）。自带依赖 `ttf-carlito 20230509-2`（Calibri 替代字体）。二进制 `/usr/bin/onlyoffice-desktopeditors`，版本 `9.4.0.129` 冒烟测试通过。可选依赖 gst-libav/gst-plugins-good 已装（内嵌视频播放），未装 libreoffice/otf-takao/ttf-ms-fonts（OpenSymbol/日文/微软字体）。desktop 文件 `onlyoffice-desktopeditors.desktop` 已注册。**主题化注意**：onlyoffice 未被 morandi-gen.py 管理，若需莫兰迪主题化须按规则扩展 `write_onlyoffice`（其主题走 `~/.config/onlyoffice/` 下 JSON/配置文件）
- **Waydroid 安装 (2026-08-09)**: `waydroid 1.6.3-1`（extra 官方源，pacman 安装 + `python-pyclip` 剪贴板支持）。**镜像下载绕过被墙的 SourceForge**：SourceForge 全节点直连/代理 <1kB/s，方案为请求 `/download` 拿 zenlayer 节点签名 URL（`zenlayer.dl.sourceforge.net/...?viasf=1&fid=...&e=...&st=...`，有效期约 1 小时）+ FlClash 代理 `curl -x http://127.0.0.1:7890 -C -` 下载到 `/var/lib/waydroid/cache_http/<filename>_<sha256>`（sha256 从 `ota.waydro.id` 的 JSON 获取），实测 500-650kB/s；system 800M（VANILLA）+ vendor 180M（MAINLINE）均校验通过后 `waydroid init` 秒过（缓存命中）。**容器**：`systemctl enable --now waydroid-container`，binder 走 binderfs（内核 CONFIG_ANDROID_BINDERFS=y，`/dev/binder` 符号链接正常）。**渲染**：Intel 硬件加速确认（`waydroid shell dumpsys SurfaceFlinger` → `GLES: Intel, Mesa Intel(R) Graphics (ADL GT2), OpenGL ES 3.2`），非 llvmpipe。**未用 NVIDIA**：hybrid 笔记本（iGPU 内屏 + RTX 3050 无任何输出端口，card0 下无输出节点）下 waydroid-nvidia 方案 README 明示 broken（gamescope 嵌套崩溃带走会话），故弃用；如后续需要 N 卡渲染只能 BIOS MUX 独显直连（COLORFUL X15 XS 22 是否有 MUX 未确认）。**坑**：pkexec polkit 认证在 niri 下卡死（`pkexec true` 也超时，polkit-kde-authentication-agent 对话框不弹），root 操作需用户手动 `sudo`；用户组只有 video（renderD12x 为 666 权限无碍）；多次超时遗留孤儿 `waydroid.py init` 进程需 `pkill -9 -f "waydroid.py init"` 清理
**Android 16 尝试失败 (2026-08-09，已停用等修复)**：装了 supechicken/waydroid-builds 20260717（lineage-23.2 GAPPS x86_64，GitHub release 直连 640kB/s 下载，资产实际指向 WayDroid-ATV/waydroid-builds 仓库）。现象：窗口闪退循环 + 容器反复冻结。诊断链：① zygote/system_server 其实能起来（GAPPS 首次 dexopt 需 3-5 分钟，之前观察太早）；② 根因是 `hwcomposer.waydroid.so` 在固定地址 `+0x38824` 反复崩溃（journalctl --user 可见 tombstone，加载 libEGL_mesa 后崩，Intel ADL GT2 iGPU）；③ `suspend_action=freeze` 会因无窗口冻结容器加剧闪退（已改 ignore）；④ WayDroid-ATV issue #25 的 workaround `ro.hardware.gralloc=minigbm_gbm_mesa` 写入 `~/.local/share/waydroid/data/misc/waydroid_settings` 后短暂正常，随后继续崩（iGPU 部分未修复，issue 只修了 dGPU）。**回退方法**：`sudo bash ~/tmp/a16/rollback.sh`（恢复 Android 13，旧镜像在 `~/tmp/a16/old/system.img.a13` + `vendor.img.a13`，Android 16 镜像被移到 `old/*.a16` 保留）。**恢复可用命令**：`sudo systemctl start waydroid-container && waydroid show-full-ui`。待社区更新构建（20260717 仅发布三周）。**已提 issue**：WayDroid-ATV/waydroid-builds issue #37（含崩溃栈 +0x38824、<composer@2.1-service> 崩溃进程、minigbm_gbm_mesa 短暂生效后复发、明确关联 #25 但指出 iGPU 未覆盖），2026-08-09 由 gh CLI 以 LanRhyme 账号发布
- **Noctalia 更新 (2026-08-16): `noctalia-git` 从 `5.0.0.r5128.gad647ae1b` 升级到 `5.0.0.r5266.g3a541270f`（=v5.0.0-beta.8-122-g3a541270fb6d，上游 main 分支）。**途径**：AUR/GitHub 因 FlClash 代理节点全部失效不可达（7897 mixed-port 直连 google/archlinux/github 全 000，节点轮换含 DIRECT 均失败，仅国内站可达）→ 改用 **archlinuxcn 源预编译包**（USTC 镜像直连）：`https://mirrors.ustc.edu.cn/archlinuxcn/x86_64/noctalia-git-5.0.0.r5266.g3a541270f-1-x86_64.pkg.tar.zst`，SHA256 `52e2721d0b35cdfa232bf9f4018056ecc8864b01f7ba37ec445efd68a1f53007` 与 archlinuxcn 数据库 desc 匹配，依赖 29 个与旧版完全一致；安装 `pkexec pacman -U --noconfirm <绝对路径>`（无 .sig 也装成功，本地文件签名验证 Optional）。**重启方式**：`pkill -f noctalia` 后 `setsid nohup noctalia > ~/tmp/noctalia-restart.log 2>&1 < /dev/null & disown`（nohup & 单独用会被 bash -c 回收不生效；kill 后 niri spawn-sh-at-startup 不重拉，需手动）。**新版本警告（待验证）**：① `~/.config/noctalia/plugins/clipboard-osd` 插件报 `missing mandatory key 'plugin_api'` 被忽略（插件 API 结构变化）② `widget.cat` 的 `noctalia/bongocat:cat` widget 类型不被识别（bongocat 桌宠 widget 可能失效）③ 社区插件源 `noctalia-dev/community-plugins` 拉取失败（网络）④ 历史更新参考：2026-08-09 r5128（paru 编译，pkexec pacman -U 产物）
- **显示管理器**: 已替换为 `greetd` + `noctalia-greeter` (Noctalia 官方原生图形化 Greeter)。通过 `greetd.service` 运行。其主题美化（壁纸、莫兰迪色板）和多显示器布局完全依赖于 Noctalia Shell 的图形界面同步功能（设置 -> Shell -> 安全 -> Noctalia Greeter -> Sync Now）。
- **主题化**: 全局由 `~/.config/noctalia/morandi-gen.py` 处理（莫兰迪色系）。避免破坏 UI 密集型应用的结构化 CSS。具体情况具体分析，部分应用主题不由其管理，像是krita。
- **Fastfetch 精致化 (2026-08-05)**: `write_fastfetch` 由局部补丁改为全量生成 `~/.config/fastfetch/config.jsonc`（模板注入 palette 色）。新布局：标题三色分区（user=text、at=iris、host=pine）、分隔线改用 `─`×40 并与 logo 等宽（色为 overlay0）、模块分三组（系统：OS/Kernel/Uptime/Packages/Shell/Terminal/Terminal Font/DE/WM；硬件：Host/CPU/GPU/Memory/Disk/Display；网络：Local IP）+ 底部 8 个圆形色点横向排列（symbol=circle，paddingLeft=2，默认 256 色索引 8-1 逆序）。图标：Host 从 `U+F2DB`（与 CPU 重复）换成 md-laptop `U+F0A58`，新增 Font `U+F0295`、Display `U+F0359`、Local IP `U+F0A60`。注意：源码中 5 位十六进制码点（≥U+F0000）必须用 8 位 `\U000FXXXX` 转义，`\U0000FXXXX`（9 位）会被 Python 截断成 `\U0000FXXX`+尾字符
- **Pi-Web 项目 (2026-08-05)**: 位于 `~/Project/pi-web`，基于 [agegr/pi-web](https://github.com/agegr/pi-web)（Next.js 16 + React 19 + Tailwind 4，端口 30141）的 pi agent Web UI，git 根提交 `b6ab21d`（225 文件）。用户反馈 lpigui（pichamber 改造）问题多、功能不完整后弃用，改用此基础。**架构**：Next.js App Router + `lib/rpc-manager.ts`（用 `createAgentSessionFromServices` 库 API 封装 AgentSession，非 RPC 子进程）+ `hooks/useAgentSession.ts`（前端会话状态）+ `components/ChatWindow.tsx`（消息/对话框渲染）。**插件适配**：`bindExtensions` 绑定自定义 `ExtensionUiContext`（pi-types.ts），完整实现 select/confirm/input/editor 对话框（ChatWindow ExtensionDialogRequest 组件）+ notify + setStatus/setWidget + custom()（库 API 模式可用，无 RPC 模式限制）；pi-go-bars setStatus 进度条直接渲染在底部状态栏。**功能**：会话浏览（直接读 pi 会话文件，含全部历史）、实时聊天流式、模型配置、技能管理（skills-service）、文件预览/编辑、Git 集成、终端、深色模式（useTheme，切换后 body #1a1a1a）、中文 i18n、工具调用详情、token/成本统计。**验证（全部通过）**：打开 1230 条消息的 agy 会话完整渲染零错误；发消息→流式回复（你好世界）；插件对话框全链路 select→confirm→input→notify（"UI 子协议全部通过"）；深色切换 #1a1a1a。**启动**：`npm run dev`（node >=22.19，本机 v26.4.0 满足），npm install 装依赖（bun install 有 integrity 错误需用 npm）。**已知坑**：npm install-scripts 安全策略需 approve sharp/unrs-resolver；首次页面编译慢（Turbopack on-demand）。**动画与功能增强（提交 072afed）**：空状态 7 个中文预设 prompt pills（探索代码库/帮我梳理进度/权衡我的选择/开始功能规划/制定一个目标/调试一个问题/审查我的改动，点击 insertIfEmpty 填充输入框）；globals.css 动画类（msg-enter 消息淡入 240ms、dialog-pop 对话框弹出 180ms、preset-pill 错落淡入、streaming-cursor 闪烁光标、chat-input-shell:focus-within 聚焦发光、全局 color/background 120ms 过渡 + prefers-reduced-motion 降级）；MessageView 消息卡片加 .msg-enter，ExtensionDialog 加 .dialog-pop，MarkdownBody isStreaming 时尾部显示 streaming-cursor。**用户环境注意**：用户浏览器装有 Dark Reader 扩展，会注入 style 导致 hydration mismatch（layout.tsx body + AppShell 根 div 已加 suppressHydrationWarning 缓解，但节点级 mismatch 无法完全抑制，建议 Dark Reader 排除 localhost）。**主题色与桌面化（提交 7640796）**：useTheme.ts 扩展 Accent 类型（sage/mist/mauve/clay/rose/olive/slate/custom 莫兰迪低饱和 8 色预设，globals.css html[data-accent] 覆盖 --accent/--accent-hover，light/dark 各自变体），自定义色用 --accent-custom 变量 + localStorage pi-accent/pi-accent-custom + layout 内联脚本恢复；AppShell 顶部栏主题色按钮弹出 fixed 定位色板（z-index 4950，含 conic-gradient 彩虹圆点和 <input type=color>）；用户气泡 --user-bg 改为 color-mix(accent 7-12%)，边框 rgba(59,130,246) 硬编码蓝改为 color-mix(accent 15-22%)；Turbopack 磁盘缓存（.next）不会因 CSS 追加失效——改 globals.css 后必须 rm -rf .next 重启否则编译产物是旧块。**Electron 桌面版**：electron/main.cjs（CommonJS，eslint 已忽略 electron/），生产模式 spawn node_modules/.bin/next start -p 30142 自动拉起并等就绪，优先复用 dev 30141，窗口 1320x860 背景跟随 nativeTheme，退出 kill next；package.json main=electron/main.cjs，scripts desktop（next build && electron .）/desktop:dev/desktop:start；验证通过（niri 显示 'lanrhyme - Pi Web' 窗口 agegr-pi-web，next prod 30142 HTTP 200）；Electron v43.3.0 已装。**深色背景加深（提交 75f397c）**：html.dark 的 --bg 从 #1a1a1a 加深为 #101014（近纯黑冷调），--bg-panel #16161c / --bg-hover #1c1c24 / --bg-selected #262630 / --border #2a2a33 / --assistant-bg #121218 / --tool-bg #15151c，layout.tsx manifest.ts electron/main.cjs 的 #1a1a1a 同步改 #101014。**启动器**：~/.local/bin/pi-web（检查 .next/server 不存在则 npm run build，然后 exec npx electron .）+ ~/.local/share/applications/pi-web.desktop（Name=Pi Web, Exec=$HOME/.local/bin/pi-web, Icon=pi-web, StartupWMClass=agegr-pi-web）+ ~/.local/share/icons/pi-web.png（复制自 public/icons/icon-512.png）。**.desktop 坑**：Exec 里不能用 $HOME（desktop-file-validate 报 'contains a reserved character $'），必须写绝对路径 /home/lanrhyme/.local/bin/pi-web；Categories 用单个主分类 Development; 避免菜单重复显示。**pi-codex-gui（Tauri 版）已删除**：~/.local/bin/pi-codex-gui、~/.local/share/applications/pi-codex-gui.desktop、~/.local/share/icons/pi-codex-gui.png、~/Project/pi-codex-gui 全部删除，残留进程（bridge.js/tsserver）已 kill，lpigui 的 vite(5173) 和 bun server(3000) 也已停；~/Project 现仅剩 lpigui（废弃保留）和 pi-web
**Pi-Web 上游同步与版本独立 (2026-08-14)**: 版本号改为独立语义化 v1.0.0（package.json，commit 23bbe5e，与上游 0.8.x 脱钩；v0.8.8 已发布 npm pi-neostudio@0.8.8 + GitHub Release 触发 CI 打包 5 资产）。**上游同步结论**：与 agegr/pi-web 无共同 git 历史（fork 是扁平根提交 b6ab21d），上游已重构核心组件（263 文件、518 提交差异），全量 merge 不可行，采用选择性移植（B 方案）。**已移植**：①pi SDK 全家桶 0.83.0→0.84.1（commit 9325c60；auth api-key 路由补 ProviderAuthInteraction.signal 参数）；②**SDK 0.84.x 回归补丁**——theme.js 的 Theme 构造在 bgColors 空对象时 bgAnsi(undefined) 崩溃（undefined.startsWith），导致 next build 收集页面数据失败+运行期崩；新增 scripts/patch-pi-theme.mjs（postinstall 钩子，fgAnsi/bgAnsi null 守卫，幂等），随 npm 包分发自动打补丁；③会话统计缓存命中率 #471（AppShell tokenRows 加 cacheHitRate 行）；④系统面板懒加载 #475（useAgentSession onSystemPromptLoaderChange + AppShell handleSystemPromptToggle，打开面板时才 get_state）；⑤瞬时会话可访问 #455（context/[id] 路由优先用活跃 RPC 会话 sessionManager，info 加 transient 字段）；⑥无最终回答过程详情默认展开 #447（ProcessDetailsGroup defaultExpanded）；⑦忽略空模型配置行 #473（models-config PUT sanitize）；⑧扩展组件收入状态栏 864b8a1（新 ExtensionStatusBar+ExtensionWidgets 货架布局，globals.css 追加 201 行样式，i18n chat.extensionWidget*）；⑨注意力通知 3d9acf6（新 lib/browser-notifications.ts + AppShell handleAttentionNeeded/deliverSessionNotification，窗口失焦时任务完成/扩展输入通知，i18n i18n.attentionNeeded/extensionInputNeeded/sessionComplete/taskFinished）；⑩图片点击预览 e851d30（新 ImagePreview.tsx + globals.css image-preview-* + MessageView 两处 img 包裹）；⑪CJK 感知 TPS 估算 a8ba47e（MessageView estimateTokens CJK 1 字≈1 token + 代理对增量缓存）；⑫skill XML 剥离 3ea687c（新 lib/slash-display.ts skillExpansionToCommand，SessionSidebar title/rename 用折叠命令）；⑬会话行上下文菜单钩子 a3f6167（新 lib/session-row-context-menu.ts 派发 CustomEvent）；⑭模型加载失败安全提示 4fc2995（withSafeModelLoadFailure）；⑮Windows 目录浏览 6960b2a（directory-browser path.posix + next.config outputFileTracingRoot）；⑯DEP0190 抑制+端口校验 16ef4a2（bin/pi-web.js spawn 结构化 argv + pi-web-options normalizePort）；⑰hydration 修复 b8a3c53（useResizablePanel mounted aria-valuemax）；⑱移动端 Enter 换行 fcfac31（ChatInput isMobile 时 Enter 换行、Ctrl/Cmd+Enter 发送，Mod-Enter keymap）。**已跳过**（规模大/与自研冲突）：0475e14 移动端工具栏重构（AppShell 1134 行）、2e9e0d6 文件查看器状态保存（747 行）、c87a9a4 分支标签（与自研用户消息导航树冲突）、c3b741e 模型配置简化（缺 models-config-store 模块）、3095198 滚动稳定化（与自研滚动修复重叠）。**发布流程备忘**：gh token 缺 workflow scope 时用 curl POST /repos/LanRhyme/pi-neostudio/releases 直接调 API（需带 token 防限流）；gh 默认解析到 upstream agegr/pi-web（因 upstream remote），必须 -R LanRhyme/pi-neostudio；git tag 默认可能开编辑器，用 git tag -a vX -m。**构建命令**：env -u TURBOPACK npm run build（shell 的 TURBOPACK=1 与 next build --webpack 冲突）；tsc 用 node_modules/.bin/tsc --noEmit（NODE_ENV=production 时 npm 会跳过 devDependencies，装依赖需 env -u NODE_ENV npm install）

**Pi-Web 会话体验增强 (2026-08-14)**: ①会话列表置顶+拖动排序（commit 000d25d，localStorage `pi-web:pinned-sessions`/`pi-web:session-order`，compareSessions 置顶优先+手动序+修改时间）；②压缩前更早对话回溯（commit 74f3ecd，新端点 GET /api/sessions/[id]/earlier?before=&limit=50 走父链，useAgentSession 单独 earlierMessagesRef 缓存、每次 loadSession/loadContext 后重前置，ChatWindow 懒加载哨兵耗尽后自动加载+滚动恢复）；③对话树完整显示：API 投影移除 MAX_PROJECTED_TREE_DEPTH=200 上限（全部用户消息保留为导航点）、BranchNavigator 分段显示（INITIAL_ROWS=500/MAX_AUTO_EXPAND_ROWS=2000/ROWS_PER_PAGE=200，'显示更多'按钮）、打开时不再自动滚到活动节点（commit cfb2bba）；④修复上翻被拉回底部（handleScrollPositionChange 去掉 !agentRunning 早退，commit b196999）+ 悬浮回底部按钮（bottom:84，commit a003f37）；⑤自定义主题滚动条 components/CustomScrollbar.tsx（overlay 式、rAF 节流、thumb 拖动、.no-native-scrollbar 类隐藏原生滚动条，接入 ChatWindow/BranchNavigator/SessionSidebar 三处，globals.css 同步）；⑥对话地图 ChatMinimap 分段加载（commit b07a473）：顶部 ↑ 按钮（earlierHasMore 时显示）点击加载压缩前轮次，轨道整体下移 topPad 避免重叠，i18n chat.minimapLoadEarlier。**待办**：用户反馈发消息时出现 pi-rewind 扩展 "Restore Options" 弹窗（Keep current files/Restore files to that point/Cancel navigation）——已实测纯发送不触发（SSE 无 select 请求），仅树导航（分支树节点/消息'从这里继续'按钮）时触发，是 pi-rewind 的 session_before_tree 钩子设计行为（~/.pi/agent/npm/node_modules/pi-rewind/src/commands.ts handleTreeRestore），等待用户确认触发场景后决定禁用弹窗/禁用扩展
**Pi-Web 渲染修复 (2026-08-13)**: ①压缩摘要默认收起——CompactionMessageView 加 expanded state（默认 false），摘要体（实测可达 335KB）不再全量渲染，展开按钮 + previewText 单行预览（i18n 新增 showSummary/hideSummary，en/zh）；②流式重复渲染修复——hooks/useAgentSession.ts 新增 streamEpochRef 世代号，rAF 节流的 message_update 在调度时捕获世代号，message_start/message_end/agent_start/agent_end/settleUiStage/handleSend 等处递增，回调执行时世代已变则丢弃，根治 message_end 后陈旧气泡把已完成消息渲染两次；③聊天懒加载可见窗口修正——ChatWindow rendered 数组里 toolResult 的 null 占位先过滤再算 getVisibleRenderWindow，向上滚动每页加载约 50 条真实消息（原先一半是 null）；④侧边栏新增「所有会话」模式——项目下拉顶部加 allSessions 项（setSelectedCwd(null)），allSessionsModeRef 防止自动选中最近项目覆盖，全部会话按项目分组带项目头展示（分组按固定项目优先 + 最近修改排序）
**Pi-Web v0.8.7 发布 (2026-08-09)**: 完成 README 重写（按 Krita 仓库风格：居中 div 头部 + for-the-badge 徽标 + 功能特性/兼容性/安装/目录结构，无句号无表情）、docs/release.md 修正为 pi-neostudio/LanRhyme 实际信息。新增 electron-builder 跨平台打包（electron-builder.yml：asar:false + npmRebuild:false，files 含 electron/.next/public，排除 !.next/cache !.next/dev，appId com.lanrhyme.pi-neostudio，buildResources=build/icon.png 来自 public/icons/icon-512.png；npm scripts dist/dist:linux/dist:win/dist:mac）。**关键修复**：electron/main.cjs spawn next 改为 resolveNextBin（打包产物无 .bin 符号链接，回退 next/dist/bin/next）+ ELECTRON_RUN_AS_NODE=1 用内嵌 node 执行（不再依赖系统 node 与 shell）。**CI**：.github/workflows/release.yml 在 release published 时触发——build-web job（ubuntu 构建 .next + rm -rf .next/cache .next/dev + upload-artifact）→ package matrix（ubuntu/windows/macos 各自 npm ci + download-artifact + electron-builder --publish never + softprops/action-gh-release 上传，必须 permissions: contents: write + GH_TOKEN env）。**踩坑记录**：① 我的 shell 有 NODE_ENV=production 导致 npm 跳过 devDependencies（node_modules 缺包→next build module-not-found），需 npm install --include=dev；② electron-builder 装包默认带上 .next/dev(928M)+.next/cache(790M) 导致安装包 900M+，排除后 AppImage 300M/deb 212M；③ windows runner 的 next build 报 EPERM scandir C:\Users\runneradmin\Application Data（Next 在 Windows 的 junction 问题），方案是只在 ubuntu 构建 .next 三平台复用；④ electron-builder 设 GH_TOKEN 后会自动尝试发布（--publish never 关闭）；⑤ softprops files 多 glob 需换行分隔；⑥ release published 事件用的是 tag 指向提交里的 workflow，tag 必须指向含最新 workflow 的提交；⑦ 重建 release 需 delete+recreate（tag 移动后同理）。**v0.8.7 已发布**：GitHub Release LanRhyme/pi-neostudio v0.8.7（5 资产：AppImage 299M/deb 212M/exe 195M/dmg x2 231M+229M），tag 指向 796f474；npm 包 pi-neostudio@0.8.7 已发布（latest dist-tag，bin=pi-web，tarball 10.3MB，README 徽标正常渲染）。**npm 发布登录要点**：① 浏览器 OAuth 登录在本机因 FlClash 代理/fake-ip 回调失败（npm 轮询 /-/v1/done 永远 202），改用 token：`npm config set //registry.npmjs.org/:_authToken=npm_xxx` ② lanrhyme 的 npm 账号开了 2FA，普通 classic token 发布报 E403，需 Granular Access Token（pi-neostudio 包 Read/write + Bypass 2FA for publishing）或发布时 --otp=6位码 ③ 发布无需 npm run build（.next 产物已在，prepublishOnly 不存在）

- **Lpigui 项目 (2026-08-05，已弃用)**: 位于 `~/Project/lpigui`，基于 [pichamber](https://github.com/AMagicPear/pichamber)（Vue 3 + Bun monorepo）改造的 pi agent GUI，git 已初始化（提交 `53fc313`+`bd71156`）。**架构**：`@lpigui/web`（Vue 3 + Vite + Pinia + Vue Router）+ `@lpigui/server`（Bun HTTP + WebSocket :3000，`@lpigui/shared` 共享类型）。**关键改造**：server 从 pichamber 的 createAgentSession（库 API，无插件 UI 事件）改为 **RpcClient（RPC 模式子进程）**——`packages/server/src/rpc-session.ts` 封装（spawn 全局 CLI `dist/cli.js`，完整事件流含 extension_ui_request）；WS 协议支持 prompt/abort/set_model/set_thinking_level/extension_ui_response。**插件适配**：前端 `ExtensionDialog.vue`（select/confirm/input/editor 模态对话框）+ `ToastHost.vue`（notify 通知），setStatus/setWidget 后台透传；**关键 bug 修复**：RpcClient.send 会自动覆盖 id（req_N），会破坏 extension_ui_response 与原始请求 id 的匹配——必须直接写子进程 stdin（LF framing JSON）。**深色模式**：`packages/web/src/styles/theme.css` 定义 CSS 变量 + `html[data-theme="dark"]` 覆盖，settings.theme 持久化（localStorage），SessionHeader 的 Appearance 按钮或 Settings 面板切换。**消息**：流式 text_delta/thinking_delta 实时渲染，agent_settled 持久化 assistant 消息，WS ready 的 messages 转历史卡片，send() 本地乐观添加用户消息。**已知坑**：① bun --hot 模式与 RpcClient spawn 冲突（会话创建卡死），dev 脚本用普通模式 ② bun-pty 原生库需 BUN_PTY_LIB 指向 `node_modules/.bun/bun-pty@0.4.10/.../librust_pty.so` ③ 5173 端口曾被旧 pi-codex-gui vite 占用 ④ cwd "~" 需解析为 homedir（rpc-session 已处理）。**验证**：CDP 端到端（发消息→USER/ASSISTANT 卡片+流式→真实模型 opencode-go/deepseek-v4-flash 显示→插件对话框 select/confirm/input 全链路→深色切换 rgb(16,18,22)↔rgb(247,247,245)），生产构建（web 858KB + server 11MB）。参考 pi-web（jmfederico/pi-web 官方 Web UI）补充了会话状态显示与模型/推理级别展示
- **Pi Codex GUI 项目 (2026-08-05 完成)**: 位于 `~/Project/pi-codex-gui`，已从 agy 接手完成并交付可用的桌面应用（git 仓库已初始化，提交 `582109b`，共 51 文件）。架构：前端 Vite+React+TS+Tailwind v4 莫兰迪暗色（OpenAI Codex 风格双栏布局）+ `server/bridge.js` 协议适配层（spawn `pi --mode rpc`，将真实 JSONL 事件转为前端友好事件：text_delta→`token`、thinking_delta→`thinking`、toolcall_*→`tool_call`、`agent_start`/`agent_settled`/`turn_end` 透传，get_commands 缓存于 `/api/commands`）+ Tauri 2 桌面外壳（`src-tauri`，Rust setup 自动 spawn bridge，bridge 带端口检测 `net.connect(9099)` 已占用则退出防重复）。**关键修复**：① bridge 与前端事件协议不匹配（agy 遗留 bug，前端原期待 token/thinking 简化事件但 bridge 原样转发）② App.tsx stale closure（streamingText 用 ref 同步）③ get_messages 真实消息 content 为内容块数组导致 React 崩溃，已归一化（`normalizeMessages` 提取 text/thinking/toolCall 块）④ ToolCard 崩溃（tool.args 可为 undefined，可选链保护 + syncToolCall 合并保留旧 args）⑤ sessionFile 误当 cwd 导致文件树为空。**插件适配**：动态加载 get_commands 全部扩展命令 chips（/cmd /menu /palette /zentui /open-tui /rewind /spawn /attach /dismiss 等），支持扩展 UI 子协议（DialogHost 组件处理 select/confirm/input/editor 并回复 extension_ui_response，notify 通知，setStatus 透传）。**功能**：模型/推理级别切换（ThinkingLevel 全级别含 minimal/xhigh/max）、流式思考折叠块、ToolCall 卡片（bash 命令+输出流 tool_output）、递归文件树+可编辑 cwd（POST /api/cwd 重启 bridge）、历史会话列表（.sessions 目录 + switch_session）、图片粘贴/上传随 prompt 发送、停用/中止按钮。**打包**：`npx tauri build` 产出 release 二进制 + deb + rpm（AppImage 因 linuxdeploy 下载失败未产出），窗口 1280x800 居中。**验证**：Edge headless CDP 端到端实测（发消息→thinking/token 流→agent_settled→历史消息渲染→bash 工具卡片 Done→18 个真实模型列表），niri 窗口实测（Rust spawn bridge + pi RPC 子进程 + 9099 监听）。注意：AGENTS.md 禁止句号规则只适用于 markdown 文档，本项目 README 已按规则编写
- **Pi Codex GUI 调试要点 (2026-08-05 追加)**: ① `{ type: 'ui_request', ...ev }` spread 顺序 bug——ev.type 会覆盖前置 type，必须 `{ ...ev, type: 'ui_request' }`（此 bug 曾导致全部 extension UI 事件（notify/select 等）在前端不生效）② pi 扩展 API 签名是 `pi.registerCommand('name', { handler: async (args, ctx) => {...} })`，UI 交互用 `ctx.ui.select/confirm/input/editor`，不是 pi.ui ③ `ctx.ui.custom()` 在 RPC 模式返回 undefined 不可用（cmd-palette 因此无法在 GUI 弹面板）④ DialogHost 的 confirm 对话框需单独确认按钮（`{confirmed: true}`）⑤ ui_request 子协议已验证：临时扩展触发 select→confirm→input 全链路，前端 DialogHost 弹出并回复 extension_ui_response，扩展正常继续
- **Ghostty 输入法修复 (2026-08-05)**: ghostty 中输入法候选框无背景模糊 + 输入时奇怪拉伸。根因：`GTK_IM_MODULE=fcitx`（niri environment 全局设置）让 ghostty（GTK4）走 fcitx5-gtk GTK 模块，该模块要求正确转发 key press/release，ghostty 不转发 key release（ghostty issue #12679，fcitx5 作者确认），导致 fcitx5 状态机异常；firefox 等用 Wayland 原生 text-input-v3 故正常。修复：`~/.local/bin/ghostty` wrapper（`exec env -u GTK_IM_MODULE /usr/bin/ghostty "$@"`），覆盖所有启动入口（niri keybinds、noctalia launcher `ghostty -e`、mango、手动命令行），已 chezmoi 管理（`dot_local/bin/executable_ghostty`）并推送；试验验证 `env -u GTK_IM_MODULE ghostty` 正常。另注：fcitx5 5.1.21 已支持 niri 的 ext-background-effect 模糊协议（issue #1560 修复），候选框模糊需主题背景带 alpha
- **Ghostty 终端背景调亮 (2026-08-16)**: `term_bg` 公式现为 `max(l_b + 0, 4)`（l_b 为 base 明度），当前背景 `#171b17`（L≈9.8，与 base #181a18 同档），已 chezmoi 同步（commit d20bf9a）。微调历史：l_b+1 初始 → l_b-6 过深回退 → l_b-1 太深 → l_b+2 太浅 → l_b+1 略浅 → l_b+0 定稿。如需调整此公式即可
- **Ghostty 终端 (2026-08-05)**: 已全面替换并替代原有 Alacritty 终端。配置存储于 `~/.config/ghostty/config`（继承字号 12、边距 16、滚动历史 10000、游标 beam/blink 及快捷键）；配置了智能快捷键（`copy-on-select = true`，选中文本 `Ctrl+C` 复制，未选中文本 `Ctrl+C` 取消任务/SIGINT，`Ctrl+V` 粘贴）；集成了多功能 Master GLSL 着色器 `cursor.frag`：① 纯净按键/移动触发闭合准星框 (仅在按键打字或光标移动时在 `iPreviousCursor` 字符处瞬时 4px 扩开渐隐) ② 统一下落扰动与拖尾/准星物理联动 (`anim_pos` 联动) ③ 全面边缘平滑与抗锯齿柔化 (`smoothstep` 径向渐变) ④ 35px 局域字符浮空下落缩放入场 (`6.0px` 下落 + `1.25x` 弹性缩小) ⑤ 70% 不透明度 0.52s 贴合光标拖尾 ⑥ 焦点获得保护 ⑦ 高级 OLED 柔光 Bloom ⑧ 暗角沉浸式边角过渡 (`vignette_strength = 0.06`)；配置 `resize-overlay = never` 关闭调整大小时窗口中央出现的尺寸胶囊弹窗；开启 `scrollbar = system` 原生滚动条支持、字体连字 (`calt`/`liga`)`gtk-titlebar = false` 极简无边框；莫兰迪配色主题生成已整合至 `~/.config/noctalia/morandi-gen.py`（输出 `~/.config/ghostty/theme`）；Niri 快捷键与 Noctalia、Mango 的默认终端命令均已彻底切为 `ghostty`，Alacritty 的配置文件与 dotfiles 已全量清理移除
- **Alacritty 终端背景 (2026-08-04)**: 背景色 `term_bg` 已调低明度至贴近黑色——公式由 `l_b + 1` 改为 `max(l_b - 1, 4)`（l_b 为 base 明度），当前背景 `#181714`（L≈7.9，保留莫兰迪暖调，仍由 morandi-gen.py 动态生成）。此前尝试 `l_b - 6`（#0b0a09）过深已回退
- **Niri 动画**: 在 `~/.config/niri/cfg/animation.kdl` 中调优为较慢、弹性轻柔的滑行（stiffness=180-220, damping-ratio=0.8），确保流畅的过渡手感
- **Niri 窗口间距**: 在 `~/.config/niri/cfg/layout.kdl` 中从 12px 缩小到 8px，优化屏幕布局空间
- **Niri 透明度**: 全局窗口规则设置 `opacity 0.98` + `blur true`（Krita 除外，另 Loupe/Kando/SPlayer 歌词也排除）。2026-08-10 从 0.9 → 0.95 → 0.98（用户偏好逐步调高）。关键点: 必须设置 `draw-border-with-background false`，否则 niri 会在窗口后方渲染为实心矩形边框，透过半透明窗口显示出来，导致聚焦时窗口看起来不透明。2026-08-10 曾尝试注释掉全部 opacity/blur 设置（用户后改主意），恢复后仅改 0.95→0.98，配置在 `~/.config/niri/config.kdl`（chezmoi 管理）
- **Zen 浏览器 (Flatpak)**: 通过 `flatpak override` 配合 NVIDIA 环境变量（`__NV_PRIME_RENDER_OFFLOAD=1`、`__GLX_VENDOR_LIBRARY_NAME=nvidia` 等）实现 GPU 加速。fcitx5 主题修复通过 D-Bus talk 权限（`org.fcitx.Fcitx5`、`org.freedesktop.portal.Fcitx`）和对 `~/.local/share/fcitx5` 及 `~/.config/fcitx5` 的只读文件系统访问实现。Zen `user.js` 已强制开启 WebRender、DMA-BUF、硬件视频解码
- **SPlayer-Next**: 通过 `splayer-next-bin` (AUR) 安装。二进制位于 `/opt/splayer-next/SPlayer-Next`，命令行启动命令 `splayer-next`。桌面文件路径已修正为 `/opt/splayer-next/SPlayer-Next`
- **Fcitx5 输入法**: 皮肤已切换为 `bamboo-dark`（古典竹简花纹主题），由 `~/.config/noctalia/morandi-gen.py` 动态注入莫兰迪配色；候选词排列已调整为竖向（`Vertical Candidate List=True`）
- **Pi Agent**: 已参考 OpenCode 完成 Pi Coding Agent (`~/.pi/agent`) 配置与深度主题/TUI 重构；安装 `pi-zentui` 与 `pi-open-tui` 扩展，配置 `zentui.json` 与 `open-tui.json`（集成 OpenCode / Claude Code 风格 ASCII 艺术 Logo Header、斜角卡片命令提示栏、固定底栏输入框、模型元数据轨、Starship 式 Git/环境状态栏、Arch 专用 Nerd 图标与百分比量规）；主题生成函数 `write_pi` 已扩展至 `~/.config/noctalia/morandi-gen.py`，全套莫兰迪 TUI 配色随壁纸动态同步
- **蛞蝓猫桌宠 (slugcatpet)**: 位于 `~/Projects/slugcatpet`，niri autostart 经 `spawn-sh-at-startup` 启动（`start.sh`，venv_sys）。全屏自动隐藏依赖 niri IPC：**niri 26.x 的 `focused-window` 输出中 `window_size` 嵌套于 `layout` 对象内（顶层无此键）**，2026-08-01 已修复 `envwatch.py` 兼容读取 layout.window_size 并保留旧格式回退；`ctypes.windll` 平台守卫拆分（win32 专属）。尺寸匹配启发式（窗口==输出逻辑尺寸）在本机无外距配置下不会误判最大化窗口（1904x1064 vs 1920x1080）。**可见表面是 GTK3 layer-shell 窗口而非 Qt 控件**：2026-08-01 实测 niri 中全屏窗口渲染于 Top 层之上、Overlay 层之下（Overlay 会盖住全屏内容），故 `gtk3_bridge.py` 已从 `Layer.OVERLAY` 改为 `Layer.TOP`，并新增 `set_visible()` 由 `envwatch._enter/_exit_fullscreen` 真实隐藏/恢复 GTK 窗口；`main.py` 用 `PetApp(QApplication)` 子类声明运行期引用（防 GC）兼类型化 `_gtk3_bridge` 等属性
- **Krita 插件 Release 资产 .desktop 修复 (2026-08-03)**: 审查发现 3 个 GitHub Release zip 缺失/过期 desktop 文件并已修复上传（5 仓库共 12 个资产全部验证含 desktop）：① Krita-AdvancedColorPicker v1.1.0 缺 `sai_vhsv_picker.desktop`（打包排除 pycache 时漏掉）② Krita-SimpleHSVSliders v1.0.1 缺 `simple_hsv_sliders.desktop`（同上）③ Krita-TVP-Timeline v1.0.0 zip 内是旧版 `kritapykrita_tvp_timeline.desktop`（Name=krita全能时间轴），已替换为仓库新版 `tvp_timeline.desktop`（Name=TVP Timeline，与 v1.1.0 一致）。修复方式：基于原 zip 解压仅增/换 desktop 后重打包（其余文件 md5 不变），经 `--resolve uploads.github.com:443:20.205.243.161` 直连真实 IP 上传（FlClash fake-ip DNS 劫持导致 uploads.github.com TLS 失败，gh 上传 EOF；直连 resolve 可行）。本地修复副本在 `~/tmp/zipfix/out/`。教训：今后打包 Krita 插件 zip 必须包含与插件同名的 `.desktop` 文件（`<name>.desktop`，内含 X-KDE-Library），Krita 靠它注册 pykrita 插件
- **Krita FolioLayers 浅色主题+配置扩展 (2026-08-03)**: 源码 `~/Projects/Krita-FolioLayers/`，安装于 `~/.local/share/krita/pykrita/folio_layers/`（chezmoi 不管理 pykrita，仓库 LanRhyme/Krita-FolioLayers，提交 `bc18869`+`99fb967`+`2cff950`+`d29530c`+`d52f358`）。完成三项：① 浅色主题适配——`theme.py` 新增 `is_dark` 明暗检测（Window 亮度<128）+ 语义色 `INFO`/`DANGER`（深色 #4299e1/#e55046，浅色 #1a6fb5/#c53030）+ `TOOLTIP_TEXT`；清除全部硬编码颜色（docker.py ToolTip #f0f0f0、layer_item 蓝/红、lucide 默认 #e0e0e0→跟随主题），仅保留 hover_preview 的 Krita 官方颜色标签语义色 ② 新增配置项：`font_size`（0=自动）、`toolbar_icon_size`（默认14，上限24）、`adaptive_layout`（默认True），settings 对话框增加对应控件（对话框 380x560）**2026-08-10 v1.2.0 发布（提交 9877b02, tag v1.2.0, Latest）**：用户确认笔拖拽正常后要求发布，不新增功能。发布内容：右键菜单三项（导出图层为 PNG——projectionImage 保留透明度 + 面板顶部 toast 提示；盖印可见图层——触发 Krita 原生 new_from_visible action；创建参考图像——create_reference_image_from_active_layer/visible_canvas 两个原生 action，均在 krita-source kis_node_manager.cpp:351/357 确认存在）；连同此前全部修复（笔拖拽 v1-v7、自适应双行工具栏、图标 48px、滤镜图层映射、落点判定、防丢失）。打包：zip 含 folio_layers/（18 文件+desktop），排除 docker_test.py、qml/ 原型、qml_list.py（未集成）、**pycache**；打包流程 ~/tmp/folio-release-120/（stage 复制 + out/Krita-FolioLayers-v1.2.0.zip），gh release create 必须在 git 仓库目录内执行。release notes 无句号无表情。用户对管理类功能提案（方案快照/组独显/隐藏辅助层等）均不感兴趣——偏好直接作用于绘画工作流的实用功能
**2026-08-10 笔修复 v7：性能优化 + 合成鼠标接管（提交 bb9bdae）**：用户反馈"笔还是无法拖拽，突然能拖也很卡"。卡顿根因：①拖拽虚影用独立 QLabel 窗口，高频 move 触发窗口重定位在 Wayland 合成器开销大；②tablet 事件 200Hz 全量转译+重绘积压。修复：①虚影改 paintEvent 绘制（grab 行 pixmap 缓存_ghost_pixmap，paintEvent 里按_pen_drag_pos 半透明 drawPixmap，与指示线同帧）；②move 事件节流至 ~60Hz（_last_pen_move_ts 12ms 间隔，节流的 move 仍被 filter accept 不双重合成）；③新增 _event_is_pen_synth（event.source()==MouseEventSynthesizedByQt）——**关键认知：niri/Wayland 可能不提供 tablet 协议，笔事件全部走 Qt 鼠标合成路径，此前这些事件回原生 QDrag 拖拽循环（QDragManager 只消费 spontaneous 事件）导致拖拽失败**；现在合成鼠标 move/release 也走自定义拖拽。④release 后调 super().mouseReleaseEvent 清理 QAbstractItemView pressedIndex。PyQt6 的 QMouseEvent 无 setSource()（只读 source()），合成路径无法 offscreen 构造验证，但读逻辑简单。offscreen 验证：30 个 move 节流 28 个、CUSTOM-DRAG/DROP/reorder 全通、幽灵清理、状态重置
**2026-08-10 笔修复 v6：丢失图层 + 拖拽失效回归（提交 eed396e, 4a455cd）**：用户反馈两个新问题。①**丢失图层**（严重）：below 分支 above_sibling=siblings[idx-1]，当被拖图层恰为目标紧邻下方时 above_sibling==drag_node 自身 → addChildNode(drag, drag) 抛异常，但 removeChildNode 已执行 → 图层从文档消失！修复：自目标保护（位置不变直接 return）+ remove 后 add 失败回滚保护（记录 old_above，失败时放回原父原位置），鼠标/笔两条路径（_execute_reorder 与 dropEvent 内联 _reorder_with_children，两处代码相同用 str.replace 全量替换）同步修复。②**拖拽完全失效回归**：v5 的_is_pen_device 改严格 pointerType 判断（只认 Pen/Eraser 名），真实 Krita 笔 pointerType 名不匹配 → 所有笔事件误判为鼠标 → 完全不转译（v4 deviceType 宽松判断时能拖，v5 完全不能，回退宽松：pointerType 或 deviceType 任一确认笔类即转译，只排除 mouse/touchscreen/touchpad）。③**拖拽落点判定**（eed396e）："中"OnItem 判定从行中间 33% 收窄到正中央 10% 窄带（仅图层组），普通图层永远只有上/下；新增 _nearest_item/_resolve_drop_target 空白落点兜底（拖到列表末尾/首部/行间隙自动吸附最近行）。offscreen 验证：stylus→pen True、core-pointer→False、幽灵生命周期、CUSTOM-DRAG/DROP 链路全过。Krita addChildNode(node, above) 语义：插到 above 视觉上方（above 在 childNodes 索引更大侧）；childNodes()[0]=最底层
**2026-08-10 笔修复 v5：拖拽虚影 + 落点重算 + 设备识别（提交 3bf65b1）**：用户新线索——"鼠标拖拽有图层虚影，笔没有"。①修复_is_pen_device：原用设备名字符串（含 mouse 即排除），真实 Krita 笔设备名若含 mouse 字样会误判为鼠标 → 笔事件退回不可靠的原生合成路径（不稳定主因）；改为 pointerType 判断（Qt6 QPointingDevice.PointerType Pen/Eraser，Qt5 int 0/1）。②自定义笔拖拽加拖拽虚影：_ghost QLabel（ToolTip|Frameless，WA_TransparentForMouseEvents，grab 行渲染图，半透明 0.85 跟随笔尖）——与鼠标原生 QDrag 虚影对齐。③_finish_pen_drop 落点改为以释放位置重算（_compute_drop_indicator 提取复用），不再用最后 move 缓存指示线；拖拽源用 _pen_drag_item 而非 selectedItems（防拖拽期间选择变化 abort）。④eventFilter 拖拽中（_pen_grab 非空）无条件转译（含树外），防释放丢失虚影/状态残留。offscreen 回归全过（鼠标路径、工具栏、ghost 生命周期）。注意：QML 测试插件 qml_layer_test 已装（~/.local/share/krita/pykrita/qml_layer_test/），行让位/落位动画只在 QML 方案里有，QWidget 主面板目前只有虚影+指示线；用户未确认是否测过 QML 插件
**2026-08-10 笔修复 v4 + QML 原型（提交 d0301cf, 34a6b58）**：用户反馈笔拖拽"少部分时候无法正常拖拽"（鼠标无问题）——根因是左滑手势误判（笔拖拽轨迹稍带水平分量即被 dx<-15 且 |dx|>|dy|*1.2 截断）；v4 改为笔路径拖拽优先（左滑仅当 |dy|<6px 且 dx<-10px 时触发），鼠标路径判定不变，offscreen 验证垂直/斜向拖拽全部 CUSTOM-DRAG、纯水平才 PEN-SWIPE。**QML 原型**：Krita 依赖 qt6-declarative + 系统 python-pyqt6（krita.so 链接 libpython3.14，插件用系统 site-packages，QtQml/QtQuickWidgets 可用）；新建 folio_layers/qml_list.py（LayerListModel QAbstractListModel + QmlLayerList QQuickWidget 包装，信号 reorderRequested/toggleVisible/rowClicked）+ folio_layers/qml/FolioLayerList.qml（ListView + DragHandler acceptedDevices Mouse|Tablet + displaced 让位动画 + 拖起放大阴影 + 插入指示线 + 眼睛按钮 + hover）；offscreen 加载 Ready；**独立测试插件 ~/.local/share/krita/pykrita/qml_layer_test/**（隔离验证，零侵入主 docker，manifest 注册 DockWidgetFactory）；真实 Krita 渲染/笔输入待用户实测，通过后再集成主 docker。注意：QQuickWidget 在 Krita 的 Wayland/OpenGL 渲染与 QML 笔输入（acceptedDevices Tablet）均未验证，这是集成前的两个最大风险点；offscreen 下 QQuickWidget 输入链路不通（sendEvent 到 QWidget window 不转发 QQuickWindow），只能靠用户实测定
**2026-08-10 笔修复 v3（提交 c3136bf）**：v2 有致命 bug——`_drag_active` 方法名与拖拽指示线 bool 属性同名，tablet 事件落入树内时 eventFilter 调 `self._drag_active()` 直接 TypeError qFatal 崩溃（真实 Krita 会崩，用户实测还是不行可能因此）。v3 彻底改方案：**自定义笔拖拽状态机**，完全绕过 QAbstractItemView 原生 QDrag——press 注入后记录选中，move 位移超阈值激活 `_pen_dragging`（事件传播会重建 QMouseEvent 丢失 setattr 标记，故用实例状态 `_pen_active`），move 实时更新插入指示线+边缘自动滚动（复刻 dragMoveEvent），release 复用 dropEvent 的 `_execute_reorder` 重排序逻辑。同时 vis_btn 移到缩略图左边（ef6e6e4）。图标上限 48px（619b0e5）。日志：FOLIO_PEN_DEBUG=1 环境变量 → ~/tmp/folio-pen.log（CUSTOM-DRAG/CUSTOM-DROP/SWIPE-TRIGGERED 记录）。注意：offscreen 无法验证真实笔设备，swipe 误判可能性仍在（SWIPE-TRIGGERED 日志可确认）；配置测试会写 QSettings（~/.config/Krita/FolioLayers.conf），测完已恢复 toolbar_icon_size=24
**2026-08-10 笔修复 v2（提交 619b0e5）**：用户实测数位笔仍无法拖拽+hover 无 tooltip。根因①hover：无按钮 TabletMove 在控件层被 Qt 吞掉（需 WA_TabletTracking 才投递），现已在全部面板控件（行/缩略图/按钮/滑动面板/工具栏）递归开启；②拖拽：注入的 sendEvent 事件非 spontaneous，拖拽循环里 QDragManager 收不到——改为 app 级拦截笔事件（不受投递目标限制），`QDrag.activeDrag()` 非空（拖拽循环中）时让路给 Qt 原生合成；③调试日志：`FOLIO_PEN_DEBUG=1` 环境变量 → `~/tmp/folio-pen.log`（记录转译/START-DRAG/dropEvent）。图标上限 14→48px（设置新增 32/40/48 档）。注意：offscreen 测试 QDrag::exec 立即返回，拖拽循环行为只能靠用户实测定；配置测试会写 QSettings，测完记得恢复
③ 自适应布局——**2026-08-10 改为双行自适应**：`docker.py` 新增 `FlowLayout`（Qt 官方示例式自动换行），宽度不足时按钮自动折行成双行/多行显示，**不再按优先级隐藏按钮**（此前 <400px 会隐藏颜色标记/图层菜单两个按钮，用户反馈缺失）；`adaptive_layout` 关闭时回退固定单行高。字号/行高/属性栏高度全部跟随配置热更新。**关键修复（99fb967）**: Krita 6 的 `Notifier` **没有** themeChanged 信号（那是 Q_SLOTS 槽，`.connect` 静默失败），真实信号在 **Window（KisMainWindow）** 上（`Window.sip` Q_SIGNALS 确认）；连接方案为 `notifier.windowCreated` → `_connect_window_theme_signal()` 连接当前主窗口 `themeChanged`，disconnect+connect 防窗口重建重复堆积；此前主题切换后插件 QSS 从不刷新，浅色下 PropCard 保持深色（近黑）、导航栏图标不可见。验证：PyQt6 offscreen 双向切换测试（PropCard #21201c→#d4d4d4→#21201c）。**2026-08-10 修复（提交 ac40be5）**：① 数位笔无法拖拽图层——根因 Qt 只在 tablet 事件未被 accept 时合成鼠标事件（Wayland 等平台不可靠），`LayerTreeWidget` 新增 `_handle_tablet_as_mouse` 把笔事件转译为 QMouseEvent 注入（按压时 `QApplication.widgetAt` 锁定抓取目标，模拟 qt_button_down 语义，accept 原事件防重复合成）；② 新建滤镜图层无效果——`add_new_filter_layer` action 在 Krita 源码中不存在（kis_node_manager.cpp 确认无此 action），`filterlayer` 改映射到 `add_new_adjustment_layer`（Krita 的滤镜图层即调整图层）；③ 顶部导航栏缺失两个按钮——自适应布局优先级颠倒，已随双行 FlowLayout 一并解决。注意：本机 Krita 6 用 PyQt6，系统 Python 无 PyQt5；测试用 `QT_QPA_PLATFORM=offscreen`；**v1.1.1 已发布（2026-08-03）**：GitHub Release `LanRhyme/Krita-FolioLayers`，标签 `v1.1.1`，资产 `Krita-FolioLayers-v1.1.1.zip`，sha256 `9fd64b0266cd50442a83125343f25ba475d964a55b8cf6bd401d0311b791740b`；发布前通过 `unzip -t`，下载回验资产 digest 一致，含根目录 `folio_layers.desktop`，排除 `__pycache__`/`.pyc`/`docker_test.py`
- **Krita Morandi-UI 插件**: 位于 `~/.local/share/krita/pykrita/krita_morandi_ui/`，源码同步于 `~/Projects/Krita-MorandiUI/`（chezmoi 不管理 pykrita）。2026 修复了 Win11+Krita 5.3.3 下动画时间轴拖动关键帧落点偏移 bug：根因是全局 QSS 中 `QTableView padding/border-radius`（+3px）与 `QHeaderView::section padding`（+8px）使时间轴内容整体偏移 ~11px，用户以标尺/docker 边框为参照拖动时产生视觉-落点错位（命中计算本身无偏移，Qt5/Qt6 双环境实测验证）。修复为 `variables.py` 新增 `timeline_exempt_style`（对 `KisAnimTimelineFramesView` 及内部 header 豁免 padding/border-radius/::item 几何），`redesign.py` 在全局 QSS 末尾拼入，实测偏移恢复至 ~0。重新发布 zip 后需让报告用户验证
- **Krita Simple-HSV-Sliders 插件**: 位于 `~/.local/share/krita/pykrita/simple_hsv_sliders/`，源码同步于 `~/Projects/Krita-SimpleHSVSliders/simple_hsv_sliders/`（chezmoi 不管理 pykrita，仓库 LanRhyme/Krita-SimpleHSVSliders）。2026 修复了 Krita 5.x（Qt5）下 `QMouseEvent.position()` 不存在的崩溃：`event.position()` 是 Qt6 API，Qt5 只有 `event.pos()`。修复为 `qt_compat.py` 新增 `mouse_x`/`mouse_point` 兼容助手（hasattr 分支自动适配），替换 8 处调用；同时补齐子目录源码缺失的 `NOTATION` 元组（安装版有而源码无）。已发布 v1.0.1（GitHub Release，zip 排除 **pycache**）
- **Krita Advanced Color Picker 插件**: 位于 `~/.local/share/krita/pykrita/sai_vhsv_picker/`，源码同步于 `~/Projects/Krita-AdvancedColorPicker/sai_vhsv_picker/`（chezmoi 不管理 pykrita，仓库 LanRhyme/Krita-AdvancedColorPicker）。本机 Krita 6.0.2.1 已启用。2026-08-01 完成 Qt5 兼容性审查并发布 v1.1.0：确认 `QEvent` 依赖 `from krita import *` 的 sip `%Import QtCore/QtGui/QtWidgets` re-export（5.3 系 kritamod.sip 源码证实，6.0 本机运行正常），`get_event_pos` 三路分支（position/posF/pos）覆盖 Qt5/Qt6，类型化枚举（Qt.WidgetAttribute/FillRule/ToolButtonStyle、QPalette.ColorRole、QEvent.Type）在 PyQt5 5.15 全部可用，Document.modified()/View.canvas()/foregroundColor() 在 5.3 系 sip 存在；加固为 qt_compat.py 显式导入 QEvent。v1.1.0 含数位板原生支持、全局事件过滤器历史落库、浮窗多屏定位与鼠标穿透、遮罩优化等 15 个提交
- **Krita InfiniteCanvas 插件 (2026-08-04)**: 源码位于 `~/Projects/Krita-InfiniteCanvas/`，远程仓库 `LanRhyme/Krita-InfiniteCanvas`（已推送）。响应用户要求已回滚代码至稳定的笔触脏标志机制（`stroke_dirty`）：画笔抬起时触发检测扩充，并在 `Ctrl+Z` 时单次联动撤销扩充与笔画，彻底解决画布重复重新扩展死循环 bug；保持单一 Release `v1.0.0` 模式并更新打包资产 `Krita-InfiniteCanvas-v1.0.0.zip`
- **Krita 插件仓库统一徽标**: 5 个仓库（AdvancedColorPicker / FolioLayers / MorandiUI / SimpleHSVSliders / TVP-Timeline）README 标题下已插入 QQ 联系徽标（链接 <https://qm.qq.com/q/mtg1yNCi1q）+> Support Me 爱发电赞助徽标（<<https://afdian.com/a/LanRhyme），格式同> MicYou，采用居中 div 头部区块排版（`<div align="center">` 包裹标题 + 徽标组，截图与正文留在 div 外）。已提交 `docs: 添加 QQ 联系与爱发电赞助徽标`、`style: 优化 README 头部徽标排版为居中布局`（后回退）、`style: 精简 README 徽标布局，统一样式并排居中` 与 `style: 居中 README 头部区块（标题、简介、截图）` 并全部推送到 GitHub（gh auth setup-git 后成功；FolioLayers 远程有新提交已 rebase）。最终版：`<div align="center">` 包裹标题 + 徽标（统一 for-the-badge，p 内并排）+ 简介 + 截图，正文（## 起）保持左对齐。SimpleHSVSliders 也已补上截图（`docs: README 头部补充截图`），5 个仓库头部结构完全一致
- **Krita-MorandiTimeline 已删除**: 2026-08-03 按用户要求删除 `~/Projects/Krita-MorandiTimeline`（CSP 风格时间轴插件，8 个提交，无 GitHub 远程，未安装到本机 Krita）。删除前已备份至 `~/tmp/Krita-MorandiTimeline-backup-20260803.tar.gz`（448K，含源码、zip、README），如确认不再需要可自行删除备份
- **Bilibili 像素弹幕助手 (2026-08-08)**: 位于 `~/Projects/bilibili-pixel-danmaku`，PySide6 桌面弹幕助手，已接入 morandi 主题（读 `~/.config/bilibili-pixel-danmaku/morandi_colors.json`，theme.py QFileSystemWatcher 热更新，morandi-gen.py 有 `write_bilibili_danmaku`）。**UI 重构（提交 style）**：QSS 全面重写为"莫兰迪像素风 2.0"——主按钮金色实色块+厚底边按压下沉（`#connect_btn`/`#save_btn`）、统计徽章改 HUD 左色条模式（PixelBadge 新增 accent_color 参数，色条=类型色）、次级按钮 2px 厚底边、输入框聚焦金色下划线、Tab 选中金色顶条、滑块金色已填充段、QGroupBox 分组、QDialog 补 bg_panel 背景；顶栏重构为三段式（标题栏 R0=标题+状态+人气、R1=连接控制、R2=统计 HUD）；VIP 列表去掉 Qt.yellow/red/cyan 纯色改莫兰迪 accent（gold/rose/purple）；弹幕卡片 gift/superchat 用 border-left 3px 色条替代整框色。**残留清理（提交 refactor）**：悬浮窗/Web 功能已废弃但清理不彻底——已删除 `ui/overlay_window.py`、`core/web_server.py`、`web/`（css/js/templates），并移除 `core/bilibili_ws.py` 中 6 处 `self.web_server.*` 死引用（该属性从未初始化，连接时会 AttributeError）；新增 .gitignore（**pycache**/pyc/.venv）并 `git rm --cached` 清除历史跟踪的 pyc。注意：PySide6 枚举需用 `Qt.AlignmentFlag.AlignTop` 等标准写法否则 pyright 报错。**界面缩放（提交 feat）**：设置新增「界面显示」tab——弹幕字号(10-24px)、头像大小(24-64px)、高能榜字号(10-24px)，存 `ui.*` 配置；`DanmakuCardWidget` 参数化 font_size/avatar_size + `set_scale()` 保存后即时刷新已有卡片，`AvatarWidget.set_avatar_size()` 动态重建，`apply_ui_scale()` 统一应用（弹幕卡片遍历 + vip_list QSS）；config.json 中 overlay 配置残留已删除。**远程**：已推送 GitHub `LanRhyme/bilibili-pixel-danmaku`（public），remote 为 https 协议。**断线重连（提交 fix ea89d7e）**：`start_async` 重构为重连循环（退避 3/5/10/15/20/30s），每次重连新建 session 并重新获取 token；`ws_connect(receive_timeout=90)` 检测半死连接（90s 无数据判失效）；新增 `reconnect_signal(delay, attempt)`，UI 状态徽章显示「重连中 N」；手动断开（stop）时 0.5s 内退出重连等待；注意 aiohttp `session.get/ws_connect` 返回的是 awaitable+contextmanager 双重对象，`async with` 不会自动 await coroutine
- **OBS 屏幕采集修复 (2026-08-08)**: 根因是 Optimus 双显卡 DMA-BUF 跨设备导入失败——niri 26.04 用核显 renderD128 (i915) 渲染，而 OBS 32.2.1 因 niri config.kdl environment 里的 `__NV_PRIME_RENDER_OFFLOAD=1` + `__GLX_VENDOR_LIBRARY_NAME=nvidia` 用 NVIDIA RTX 3050 渲染（日志 `Loading up OpenGL on adapter NVIDIA`），niri 分配的 linear buffer 在 NVIDIA 上只能 EGL_EXTERNAL 导入而 OBS 用 GL_TEXTURE_2D，导致 PipeWire 协商失败（OBS 日志 `no more input formats`，niri 日志 `wrong modifier choice type`） 采集链路本身正常（OBS → xdg-desktop-portal → xdg-desktop-portal-gnome（niri 依赖，勿删）→ niri 模拟的 `org.gnome.Mutter.ScreenCast` → PipeWire） 修复：覆盖 `~/.local/share/applications/com.obsproject.Studio.desktop`，Exec 改为 `env -u __NV_PRIME_RENDER_OFFLOAD -u __GLX_VENDOR_LIBRARY_NAME __EGL_VENDOR_LIBRARY_FILENAMES=/usr/share/glvnd/egl_vendor.d/50_mesa.json obs`，强制 OBS 走 mesa EGL 用核显 验证：隔离 user-data 测试实例日志 GPU 变 `Intel Mesa Intel(R) Graphics (ADL GT2)`，pipewire stream `streaming (error: none)`（NVIDIA 下是 `error: no more input formats`） 代价：NVENC 编码器不可用，直播/录屏需改用核显 QSV（iHD 驱动与 vpl-gpu-rt 已装）或 x264 备选方案（未采用）：niri config.kdl 加 `debug { render-drm-device "/dev/dri/renderD129" }` 可保留 NVIDIA 渲染，但显示器全部接在核显（card1-eDP-1 + card1-HDMI-A-1），NVIDIA 渲染 + PRIME 输出到核显有风险且违背 offload 偏好 另注：grim 截图正常（走 wlr-screencopy 协议非 portal 路径），portal 屏幕共享仅 OBS 受影响
- **obs-bilibili-stream 登录修复 (2026-08-08)**: 手动源码编译安装于 `~/.config/obs-studio/plugins/bilibili-stream-for-obs/bin/64bit/`（非 AUR，AUR 包 obs-bilibili-stream 已过时停在 2.0.12） 扫码登录失效根因有两个：① B 站 2026-08 改版，扫码登录成功 URL 变为 crossDomain ticket 格式（`passport.biligame.com/.../crossDomain?ticket=...`），不再内嵌 SESSDATA/bili_jct/DedeUserID，旧解析逻辑拿不到 cookie ② B 站 API 走 HTTP/2，响应 header 名小写 `set-cookie:`，插件 headerCallback 用大小写敏感 `strncmp` 匹配 `Set-Cookie:` 永远漏掉，response.cookies 恒为空 → 最后一步「无法获取登录 Cookies」失败（扫码/确认其实都成功） 修复：应用上游 PR #27（Zarosmm/obs-bilibili-stream，作者 chengjilai，未合并）——`strncasecmp` 大小写不敏感 + cookies 为空时跟随 crossDomain ticket URL 从其 Set-Cookie 获取登录态，保留旧 URL 参数解析作 fallback 本地源码在 `~/tmp/obs-bilibili-stream/`，补丁文件 `/tmp/pr27.diff`，旧版备份 `~/tmp/bilibili-plugin-2.1.0.bak.so` 注意：上游未合并该 PR，插件升级后需重新打补丁（编译用 `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DENABLE_FRONTEND_API=ON -DENABLE_QT=ON`，安装到用户插件目录 bin/64bit）

## PiDeck 桌面工作台（已卸载，2026-08-13）

- 曾安装 [ayuayue/PiDeck](https://github.com/ayuayue/PiDeck) v0.6.7（Electron 22 的 pi 多会话工作台）至 `~/Applications/pi-desktop-0.6.7/`，用户试用后觉得没用，当日已完全卸载（应用目录/启动器/.desktop/图标/配置 `~/.config/pi-desktop`/4 个 pi-deck-* 扩展/chezmoi 源均已清除并推送，勿重复安装）
- **若将来重装**，关键经验：① 下载需 `--resolve` 直连 US IP 140.82.114.4 + `-C -` 续传（亚太 IP TLS 被重置）；② 启动必须设 `PIDECK_LINUX_DISPLAY_BACKEND=wayland`（默认 XWayland 在 niri+xwayland-satellite 下窗口不映射）；③ npm/electron 上下文继承的 `ELECTRON_RUN_AS_NODE=1` 会让二进制静默退出

## MicYou 安装状态 (2026-08-09)

- **分支**: `feat/plugin-system`，版本 `2.0.0-alpha.1`（code 23），工作区干净
- **手机端**: `./gradlew :composeApp:assembleDebug` 产出 `composeApp-debug.apk`，`adb install -r` 已装到 Redmi K70（ed3fdd92），MainActivity 启动验证通过（无签名环境变量，release 需 ANDROID_KEYSTORE_*）
- **桌面端**: `npm run tauri build` 从源码构建（Rust release 5m04s + 前端），产出 deb（AppImage 因 linuxdeploy 下载失败未产出，已知网络问题）
- **安装方式变更**: 旧 AUR 包 `micyou-bin 1.3.5`（他人维护）已卸载，替换为本地构建包 **`micyou 2.0.0.alpha.1-1`**（`~/tmp/micyou-local/PKGBUILD`，基于 deb 提取：`/usr/bin/micyou-app` + `/usr/lib/MicYou/resources`，`micyou` 为符号链接指向 GUI，LICENSE 装到 `/usr/share/licenses/micyou/`）。PKGBUILD 以本地 deb 为 source，`pkexec pacman -U` 安装（sudo 需密码）
- **验证**: 启动冒烟测试通过（niri 窗口 Title=MicYou, App ID=micyou-app），`~/.config/micyou/` 含 plugins/plugin-state.json（插件系统已就绪）
- **注意**: 升级新版时先 `pkexec pacman -R --noconfirm micyou-bin` 再装新包（conflicts 交互提示在 pkexec 下无法应答）

## MicYou Wi-Fi 连接问题修复 (2026-08-09)

- **症状**: Linux 下 Wi-Fi 模式手机连不上电脑，Windows 正常
- **根因**: UFW 防火墙 active（默认 deny incoming），只放行了 KDE Connect 的 1714:1764，MicYou 端口全被静默丢弃；Windows 首次运行会弹防火墙允许窗口故无此问题
- **修复**: `pkexec bash -c 'ufw allow 8554/tcp && ufw allow 8555/udp && ufw allow 8443/tcp && ufw allow 5353/udp'`（8554 TCP 控制 + 8555 UDP 音频 + 8443 web 模式 + 5353 mDNS 发现），IPv4/v6 均添加
- **验证**: 手机 `nc -w 3 192.168.1.9 8554` 连接成功；用户实测正常连接
- **排查要点**: MicYou 服务端本身正常（监听 0.0.0.0:8554/8555、avahi 发布 `_micyou._tcp` 192.168.1.9:8554），先查 `ufw status`、`ss -tlnp`、`avahi-browse -rt _micyou._tcp`、确认手机与电脑同网段

## Krita G'MIC 滤镜插件 (2026-08-11)

- **背景**: 用户询问 Krita 是否有滤镜插件，已安装官方 `extra` 源的 **`krita-plugin-gmic 3.7.4.1-3.1`**（自带依赖 `gmic 4.0.3-2.1` 与 `graphicsmagick`），G'MIC 提供 800+ 滤镜（艺术/油画/卡通/降噪/HDR 等）
- **安装方式**: `sudo pacman -S --noconfirm krita-plugin-gmic`（sudo 需密码，pkexec 在 niri 下会卡死）
- **插件类型**: 原生 `kritaplugin`（非 pykrita），文件在 `/usr/lib/kritaplugins/krita_gmic_qt.so`，滤镜数据在 `/usr/share/gmic/`
- **启用方法**: 需重启 Krita，在 设置→配置 Krita→插件管理器 勾选 GMic 启用

## Krita GmicFilters 滤镜中心插件 (2026-08-10)

- **项目**: `~/Projects/Krita-GmicFilters/`（git 已提交，LanRhyme 风格），安装于 `~/.local/share/krita/pykrita/gmic_filters/`（chezmoi 不管理 pykrita）
- **功能**: G'MIC + Krita 原生滤镜中文面板，搜索/分类/收藏/参数记忆/自定义命令，18 个 G'MIC 滤镜（gmic 4.0.3 实测）+ 9 个 Krita 原生滤镜
- **架构**: `__init__.py`（DockWidgetFactory 注册，DockRight）+ `docker.py`（主面板）+ `filter_db.py`（滤镜库）+ `gmic_runner.py`（CLI 执行与图像往返）+ `pixels.py`（像素<->QImage，8/16 位 RGB/RGBA/GRAY）+ `gmic_detector.py`（跨平台检测）+ `qt_compat.py`（PyQt5/6 兼容）+ `config.py`（QSettings：收藏/参数记忆）
- **关键技术点**: ① gmic 会把 `-o` 输出选项吞成滤镜参数——所有内置滤镜必须带完整显式参数，且用 `-o[0]` 定点输出 ② 图像往返走临时 PNG（区域像素→QImage→gmic→写回 setPixelData，撤销由 Krita 记录）③ 检测 gmic 走 PATH+常见安装目录+手动路径，10s 缓存 ④ QSettings INI 返回 str，时间戳需 float() 强转 ⑤ PyQt6 槽内未处理异常会 qFatal 中止，参数面板 JSON 损坏需防御（get_params 校验 dict 类型）
- **关键坑**: pykrita 插件的 `.desktop` 必须放在 `~/.local/share/krita/pykrita/` **根目录**（`<id>.desktop`），不能放插件目录内；格式必须用 `ServiceTypes=Krita/PythonPlugin`（非 X-KDE-ServiceTypes）+ `X-Python-2-Compatible=false`（非 X-Python-Version）。放错位置插件列表完全不显示；**Docker 类必须继承 `from krita import DockWidget`（QDockWidget）**，内容用 `setWidget(main_widget)` 装入，否则报 `TypeError: invalid result from DockWidgetFactory.createDockWidget()`
- **状态**: 离屏 UI 测试全过，18 滤镜端到端执行全过；desktop 位置/格式已修复，**待用户重启 Krita 确认插件出现在列表**（kritarc 已写入 `enable_kritapykrita_gmic_filters=true`，备份在 ~/tmp/kritarc.bak.*）
- **已知限制**: gmic 同步执行会短暂阻塞 UI（大图慢）；原生滤镜参数是尽力而为（属性名不匹配时用 Krita 默认参数）；drop_shadow/frame 等会改变输出尺寸，自动缩放回原区域

## ReveriePaint-native 笔刷引擎 (2026-08-13)

- 阶段1完成: Krita 真实笔刷引擎集成 (commit 622cf58/2369cdf/2eb13f0)
- 关键机制: 预设 .kpp 是自包含 PNG (内嵌 XML+笔刷), 16个在 krita/data/paintoppresets
- 注册: register_static.cpp 编译进 kritadefaultpaintops_static, 避免模板跨 DSO vtable 错位崩溃
  (在 harness 侧实例化 KisSimplePaintOpFactory 会让 kritaimage 内部虚调用错位段错误)
- 渲染: KisBrushOp::paintLine + KisPaintInformation(压感) + KisFakeRunnableStrokeJobsExecutor
  同步驱动异步 dab 管线 + doAsynchronousUpdate 取 ready dabs bitBlt (Krita 测试同款机制)
- 桌面 harness 验证: 压力1.0->0.3 线宽38->2px, 软边正常 (offscreen + QT_FORCE_STDERR_LOGGING)
- QApplication 在无头环境需 QT_QPA_PLATFORM=offscreen, 否则段错误
- CMake 链接: kritalibpaintop(kritaui 依赖, APK +12MB) + kritadefaultpaintops_static
  - kritacolorsmudgepaintop.so (MODULE 库无 lib 前缀, 需复制 lib 前缀版供 -l 链接,
    AGP 会把 target_link_libraries 的 .so 路径剥成 -l)
- AGP 自动收集 CMake NEEDED 依赖到 cxx obj (33个), jniLibs 只保留 AGP 漏的 14个
- 笔刷参数: setPaintOpSize/Opacity/Flow, JNI/Kotlin/UI 全链路, 面板含缩略图网格
- 16 预设全部可加载 (15 paintbrush + 1 colorsmudge)
- 待: 真机验证(手机离线), 阶段2工具系统(KisTool 评估), 硬度/间距滑块(brush_definition XML)

## Krita-MobileUI 项目 (2026-08-11)

- **项目**: `~/Projects/Krita-MobileUI`,为手机和平板打造的 Krita 移动端 UI,目标类似画世界 Pro + CSP 手机版双模式
- **基础**: 官方 Krita QML tablet UI 原型 MR 2417 (timotheegiet, 321 文件),已从 invent.kde.org 的 timotheegiet fork 下载(proxy 127.0.0.1:7890 走 FlClash)
- **架构**: 纯 QML UI(kritaTabletModule)+ 未来 C++ 核心集成。双模式:
  - Simple 模式:手机竖屏,全屏画布+浮动底部栏,底部弹出 bottom sheet 面板,手势(捏合/边缘热区)
  - Studio 模式:平板横屏,侧边工具栏+面板 dock
  - uiMode 状态在 SharedProps (auto/simple/studio),auto 按屏幕尺寸检测(600px 阈值)
- **关键文件**: `tabletUI/TabletUITest.qml`(qml 直接运行测试版)/ `TabletUI.qml`(编译版)/ `SimpleCanvasScreen.qml`(手机布局)/ `CanvasScreen.qml`(平板布局)/ `coreQML/SharedProps.qml`(全局单例)/ `coreQML/PanelSlide.qml`(滑入滑出)/ `coreQML/Pressable.qml`(弹性按压)/ `coreQML/GestureLayer.qml`(手势)/ `coreQML/SimpleButton.qml`(弹性反馈)
- **运行**: `cd tabletUI && /usr/lib/qt6/bin/qml -I . TabletUITest.qml`(注意 PATH 里 qml 是 Qt5 的,必须用 /usr/lib/qt6/bin/qml);编译版 `cd tabletUI/build && cmake .. && make && ./kritaTablet`
- **验证**: 双模式自动检测/手动切换全过;Simple 模式 400x800 加载无错;毛玻璃面板(高斯模糊 24px + 0.85 透明度)渲染正常;编译版运行正常
- **已知坑**: ① qml 工具吞 console.log 输出,需 QT_FORCE_STDERR_LOGGING=1 + console.warn ② 图标资源已从损坏符号链接转为真实文件(原链接指向 ~/Projects/krita-source 的绝对路径,不可移植)③ MultiPointTouchArea 只有 touchUpdated 信号无 onTouchReleased ④ QtObject 不能直接含 Connections(需 Item 基类)⑤ layer.effect 需直接子级 GaussianBlur 实例
- **Android 构建**: buildAPK.sh 需要 Qt for Android(未装)+ Android SDK(已就绪: ~/Android/Sdk, android-36, NDK 25/27);官方 MR 测过 Qt 6.6.3 + NDK 25.1 + android-33
- **待办**: 阶段二手势完善、阶段三 C++ 集成、阶段四 Android APK、阶段五发布(详见 docs/ROADMAP.md)
- **设计规范**: docs/DESIGN.md(莫兰迪配色、动效时长、触控规格)

### 2026-08-12 22:40 手势三连根因(已修, commit c577c54)

- 单击变双击根因:awaitLongPressOrCancellation 内部吞掉 UP 事件,返回 null 后 else 分支永远等不到 up,第一次点击丢失,第二次 down 才触发 onClick
- 左滑根因:同一 UP 被吞问题 + 抽屉 AnimatedVisibility modifier 里 fillMaxSize 覆盖了 width(全宽)
- 组缩进根因:Spacer(缩进)放在缩略图之后(名称右移),应放行最前(整行右移),depth*16dp
- 新手势:完整事件流自处理(不用 awaitLongPressOrCancellation)——maxMoveSq 累计移动,elapsed>=400ms 且 maxMoveSq<=1.5x slop → 长按拖拽;movedSq>slop → 左滑;up 时 maxMoveSq<=slop → tap
- C++ depth 语义:walk(root,0) 顶层 depth=0,组内=1;显示算法 collectBlock(0,n,-1)
- 手机曾锁屏(NotificationShade 卡住),cmd statusbar collapse 无效,靠用户自行解锁

### 2026-08-13 00:10 手势与交互修复 (commit bfe5249)

- 新问题修复:点击已选中行的眼睛/折叠箭头会进二级页面 —— 根因:行手势覆盖整行,子元素(眼睛/箭头)点击时行手势也判定 tap;修复:interactiveEndPx 区域(缩进+箭头22dp+眼睛28dp)内的 down 直接 return@awaitEachGesture 交给子元素
- 左滑/长按参数放宽:长按 slop 1.5x→2x touchSlop,左滑阈值 10dp→8dp
- adb 实测验证过(bfe5249 前版本):左滑触发 SWIPE 日志+抽屉出现,长按触发 DRAG start/end;用户反馈仍不行,可能因从行左侧(眼睛区)操作或幅度不足
- 拖拽落点日志:dragPos y=.. over=.. 已埋,用户测试时可抓 adb logcat -s LayerPanel
- adb 测试流程(手机解锁时):tap 540,2300(+创建) -> tap 540,737(9:16预设) -> tap 849,137(图层按钮)
- 注意:手机易锁屏(NotificationShade),adb 操作前检查 mCurrentFocus;用户可能在用手机时勿操作

### 2026-08-13 00:40 长按根因确认与修复 (commit 52d5aed)

- 长按拖拽失败真正根因:按住不动时系统不发 move 事件,时间戳检测(事件驱动)永不触发;adb 原地长按日志为空证实
- 修复:回到 awaitLongPressOrCancellation(内部 suspendCancellableCoroutine+withTimeout,事件无关超时);长按成功时 performHapticFeedback(LongPress) 震动(画世界 Pro 逻辑:长按震动后可拖拽)
- up 吞修正:awaitLongPressOrCancellation 返回 null 后,单击的 UP 已被吞,else 分支等不到 up → 挂起直到下一手势 down(change.id 不匹配 → break,无害);swipe 的 move 未被吞,正常累计
- 眼睛进二级页面根因:interactiveEndPx 漏算行内 8dp padding,眼睛右边缘落在手势区;修复 = 8dp padding + 缩进 + 箭头22dp + 眼睛28dp + 4dp 余量
- 手势最终结构:交互区跳过 -> awaitLongPressOrCancellation -> 长按(震动+拖拽)/ null(swipe 累计 reveal / tap 判定)

### 2026-08-13 01:20 手势最终方案 (commit 788b5fd)

- 决定性证据(logcat):用户长按拖拽其实触发正常(DRAG start haptic / dragPos y=619..334 / drag END),但 dragPos over=-1 null 全程 —— rowBounds 从未被填充(LayerRow 的 onGloballyPositioned 只更新本地 rowTop,没写列表级 rowBounds map)→ 落点永远算不出 → moveLayer 不执行
- 左滑无 SWIPE 日志:awaitLongPressOrCancellation 检测移动时吞掉 move+up 事件,else 分支等不到事件 → 挂起 → 快速短滑全丢
- 最终手势结构:
  - 左滑:行上 detectHorizontalDragGestures(官方,先声明先收事件,consume 防 clickable 取消)
  - 点击/长按:combinedClickable(onClick=选中/详情, onLongClick=震动+onDragStart)——官方实现,长按计时事件无关(按住不动也能触发)
  - 拖拽移动:列表 Column 上 pointerInput(draggingFrom) + awaitPointerEventScope 循环(长按激活后接管后续 move,consume 防 verticalScroll 劫持),onGloballyPositioned 记 columnTop 换算全局 y
  - rowBounds 填充:LayerRow 加 onBounds(top,bottom) 回调 → rowBounds[index]
- 关键 API 坑:PointerInputScope 不继承 AwaitPointerEventScope,pointerInput 块内直接 awaitPointerEvent 报 Unresolved,必须 awaitPointerEventScope 包裹
- 拖拽视觉反馈(动画一直都在,之前被 bug 掐死):dragLineAbove 插入线 2dp accent / dragOnGroup 组悬停 accent 30% 背景
- 用户催"动画和 UI"——动画代码存在但 never triggered,修复后可见

### 2026-08-13 01:50 拖拽/左滑/动画 (commit 已提交)

- 拖拽"没效果"根因:只有2层(背景+颜料图层1)时,唯一拖拽目标=背景行 → C++ moveLayer guard 拒绝 toIndex=0 → 永不移动;修复:endDrag 里 target.first<=0 时 to=1(背景上方最低可动位置)
- 图层移动动画:LazyColumn + items(key)+ Modifier.animateItem()(LazyItemScope 成员,无需 import;animateItem 是成员函数不是顶层扩展)
- 左滑最终方案:行上完整事件循环 awaitFirstDown(requireUnconsumed=false)+ swiping 后 consume move(与 combinedClickable 共存,通过 consume 仲裁:swipe 消费 move → clickable 取消;tap 无移动 → clickable onClick;长按无移动 → clickable onLongClick)
- detectHorizontalDragGestures 被弃用:与 combinedClickable 事件分发顺序冲突(down 被 clickable 消费后 horizontal 拿不到)
- LayerRow 加 modifier 参数(animateItem 挂在根 Box)
- 列表分隔线删除(divider 逻辑随 for 循环移除)

### 2026-08-13 02:10 画世界Pro式拖拽动画 (commit 28b25a6)

- 用户明确要的拖拽动画:长按浮起 -> 跟手移动 -> 其他行让位 -> 松手插入
- 实现:
  - displayList:拖拽时被拖行临时移到 dragTargetIdx(LazyColumn items + animateItem 自动让位动画)
  - updateDragPos 算两件事:target(行中点之上=插入位)+ over(插入线/组高亮)
  - LayerRow isDragging/dragFingerY 参数:graphicsLayer(translationY=手指-行中心, scale 1.05, shadowElevation 12dp, alpha 0.96)+ zIndex 2 浮起
  - 松手 endDrag:真实 moveLayer -> vm.layers 更新 -> displayList 收敛(无跳变)
- 左滑:方向检测(swipe 后 dx<0 即 reveal,不再累计阈值)+ DOWN/SWIPE 日志(android.util.Log.d)
- 抽屉改 3 按钮:复制/独显(soloLayer)/删除(FolioLayers 风格,用户插件就是 3 按钮)
- 待验证:左滑仍不显示则从 logcat 'LayerPanel' DOWN/SWIPE 日志定位(用户测试时抓)

### 2026-08-13 02:40 拖拽统一 (commit da63191)

- 用户 4 反馈:动画位置与实际插入不一致 / 松手选中被动行 / 可拖到背景下方 / 拖拽需 z 置顶显示虚影
- 统一方案:单一目标 dragTargetIdx(显示插入位);endDrag 里 to = displayList.size - 1 - insert(displayList 视觉 top-first = m_layers 反转,计算在清理 drag state 前)
- 背景保护:updateDragPos 里 clamp target <= bgVisual-1(bgVisual = displayList 中 index==0 的视觉位置)→ 被拖行永不落背景之下
- 松手选中被拖行:endDrag 里 selectedIndex = from + vm.setCurrentLayer(from)
- z 置顶:zIndex(10f) 移到 LayerRow 根 Box(modifier 开头);原 zIndex 在 Row 上(Box 内兄弟无效果,阴影被遮)
- 插入线删除(让位动画即指示);保留组中间 30%-70% 区域 OnGroup 拖入(moveLayerToGroup)
- to 计算验证:拖到视觉顶=3 / B 上方=2 / bg 上方=1(from==to 拒绝=无操作)全部一致;组内插入靠 C++ moveLayer 跨父处理

### 2026-08-13 03:10 拖拽/左滑最终修复 (commit 1697919)

- logcat 决定性证据:SWIPE reveal 日志触发但抽屉不显示 → 抽屉 UI 问题(AnimatedVisibility 在 LazyColumn row 内不可靠)→ 改常驻 Box + graphicsLayer alpha = revealFraction(行 offset 滑开露出)
- 拖拽落点与动画不一致根因:updateDragPos 基于"重排后 rowBounds"有滞后 → 改数学映射 target = (fingerY - columnTop)/rowPx(行高固定),与 animateItem 让位完全同步;endDrag to = displayList.size-1-insert(先算后清理)
- 浮起被遮挡:zIndex 在 LazyColumn item 内无效 → overlay 方案:LazyColumn 包进 Box,拖拽时列表内被拖行 alpha 0.4,Box 顶层渲染 LayerRowContent 浮起副本(offset 跟手 + scale 1.05 + shadowElevation 16dp)
- LayerRowContent 抽取(行视觉:缩进线/箭头/眼睛/缩略图/名称/状态),LayerRow 只留手势(Box 上 pointerInput 左滑 + combinedClickable 点击/长按)+ 抽屉 + 内容调用
- 背景保护:target clamp 到 bgVisual-1;拖到背景行上方 = 最低可动位置
- 左滑手势(在 Box):awaitFirstDown(false) 完整事件循环,swipe 后 dx<0 即 reveal
- git checkout 陷阱:checkout 恢复后之前的未提交修改全丢(抽屉/target/overlay 各做了一次半)
- Kotlin 文件按行号重写陷阱:括号计数从 Row 的 ') {' 行开始(open_idx=va+2),@Composable 前缀保留(lines[:start-1] + new 含@Composable),lines[end-1:] 保留 DrawerAction 的 @Composable

### 2026-08-13 03:35 抽屉命中/拖拽回跳修复 (commit 78f0d16)

- 用户反馈:未左滑点击按钮区域仍有操作(常驻抽屉 alpha=0 时仍命中)+ 拖拽动画位置与实际不符
- 抽屉修复:if (reveal) 才组合(闭合行不渲染不命中),reveal 后 alpha 淡入;之前常驻 Box + graphicsLayer alpha=0 的方案命中测试不受 alpha 影响(用户没左滑就能点到按钮)
- 拖拽"动画位置不符"真正根因:松手瞬间 draggingFrom=-1 → displayList 回原始顺序 → 被拖行先飞回原位(动画)再飞到落点(第二段动画),用户感知位置不符
- pendingOrder 冻结:endDrag 里 moveLayer 前 pendingOrder = displayList.map{it.index}(冻结拖拽临时顺序),displayList 优先 pendingOrder,LaunchedEffect(vm.layers) 释放(数据落地后无跳变)
- 数学映射 target=(fingerY-columnTop)/rowPx + to=size-1-insert 已验证正确(拖顶=3/B上=2/bg上=1)

### 2026-08-13 04:10 拖拽精确对齐+抽屉渲染修复 (commits e4dc82b, f6a7400, e35248b)

- Krita moveNode(node, parent, newIndex) 真实语义(读 kis_node_facade.cpp):aboveThis = parent->at(newIndex-1)(移除前),add 到 aboveThis 上方 → node 落 aboveThis 移除后的 index;aboveThis==node 时直接 return false(无操作)
- 落点下一格根因:moveNode 的"上方"是 m_layers 小索引方向(视觉下方),insert+1 方向搞反;正确:to = size-1-insert,向上拖(to>from)用 aboveThis = m_layers[to+1](越界→-1=树顶 lastChild),向下拖(to<from)用 aboveThis = m_layers[to]
- 松手选错层:selectedIndex=from 在移动后索引变化,应 selectedIndex=to(移动后新索引)
- moveLayerAbove(from, aboveIndex) 支持 aboveIndex==-1(加到 root 顶部,aboveNode=parent->lastChild())
- logcat 决定性证据:SWIPE reveal idx=2 dx=-269..-417 触发正常 → 左滑手势没问题,是抽屉渲染问题
- 抽屉修复:去掉 graphicsLayer alpha 淡入(疑似动画卡0,按钮可点不可见),行滑开 offset 改用 reveal 布尔(-drawerPx)不依赖 revealFraction 动画值

### 2026-08-13 05:10 拖拽浮起跟手恢复 (commit 8730195)

- 用户反馈:左滑按钮终于显示(zIndex 置顶修复生效),但浮起层吸附槽位版"动画没了"、落点问题延续
- 关键领悟:用户要画世界Pro式"浮起跟手"(连续)而不是吸附槽位(跳变);落点 = 让位槽位(四舍五入 insert)
- 落点数学链路(已验证全场景一致):insert 四舍五入(行上半插上方/下半插下方) -> to = displayList.size-1-insert -> aboveIdx = to>from ? to+1(越界-1=树顶removeNode+addNode) : to -> moveLayerAbove
- 坐标空间:columnTop(LazyColumn boundsInRoot.top) == listTop(外层Box top),浮起 offset 用 dragFingerY - listTop - rowPx/2 跟手
- 拖拽时行Box左滑pointerInput也会consume移动(swiping=true),但不影响面板级pointerInput(draggingFrom)接收事件(consume只标记不阻断)

### 2026-08-13 05:40 拖拽落点真凶找到 (commit 965a6fc) — 桌面harness 9/9 PASS

- KisNode::add(newNode, aboveThis) 真实实现(kis_node.cpp:469):idx = index(aboveThis) + 1,node 插到 aboveThis 上面一格(不是替换 aboveThis 位置)
- 之前 to+1/to 公式的方向假设错(aboveThis 位置语义),向上拖总差一格
- 正确公式(harness 9 场景全验证):to = size-1-insert;向上拖(to>from) aboveIdx = to;向下拖(to<from) aboveIdx = to-1;to==from 无操作
- 树顶特例不再需要(to=size-1 时 aboveIdx=to=最顶层,add 到它上面=最顶)
- harness 编译命令:g++ -std=c++17 -fno-operator-names -fPIC + Qt6/KF6(-I/usr/include/KF6 + 各 KF6 子目录)/krita libs 头文件/build-desktop 生成头(KoConfig.h, *_export.h)/Imath/half.h 等
- newDocument 现在自带 bg+颜料图层1 两层,harness 需 removeLayer(1) 后加测试层

### 2026-08-13 06:10 拖拽后幻影动画根因 (commit 77ace29)

- 用户:拖拽落点正常了,但松手后列表又播放一遍动画(行错位重排)
- 根因:pendingOrder 冻结顺序用 List<Int>(index) 存储,但 moveLayerAbove 落地后层索引变化(如 A 从 index1 移到 index2),恢复时 byIndex 按新索引映射错位(A/B 交换)触发 animateItem 幻影动画
- 修复:pendingOrder 改存 List<String>(层名,移动后不变),恢复用 byName(associateBy name);同名层罕见可接受
- pendingOrder 目的:松手后到 vm.layers 更新前保持拖拽顺序(避免行弹回原位再跳落点的双动画)

## ReveriePaint-native

### 2026-08-15 手势/滤镜/性能轮次（agy 会话 9b61300c 中断后接续）

- **关键修复（5712147）**：手势 release 处理原先在每次 down/move 事件执行（up 时 `pressed.isEmpty()` 直接 break 跳过），导致①每个 move 都触发 touchEnd 笔画无法连续绘制②拾色状态在抬手后残留。已把 release 块（touchEnd/拾色清理/双指三指撤销判定）移到抬起分支内，adb 实测事件序列修复
- **取色偏移开关（2ea9a59）**：`eyedropperOffsetEnabled` 默认 true，取色点偏移 -48dp 防手指遮挡，设置面板颜色设置可关
- **长按取色修复（074acbd）**：原激活检查只在 move 事件执行，按住不动无事件到达→取色器不出现；现 down 后启动延迟协程（独立 Main scope，delayed 到点激活，抬手/双指取消），保留 move 兜底
- **独显系统性重构（c8c43b3）**：彻底改为纯渲染端过滤——solo 不再修改任何 node 状态（visible/opacity/blend/inheritAlpha 零触碰），删除了 SoloBackup 快照/恢复机制；renderToBuffer 独显时用 compositeSoloProjection 手动合成 keep 层（目标+祖先+后代+背景，按栈序），raw mode 也是合成参数；solo 状态用 node 指针（免疫 index 漂移），sync 时目标被删自动取消否则重算 keep；图层面板非 keep 行仅 UI 显示隐藏；JNI 新增 soloActive/layerSoloKeep；删除废弃 direct 设置方法
- **同会话早前完成**：保存/载入加载中状态+二次确认+tabler 图标统一、滤镜面板外部点击不关、高斯/动感模糊黑边修复、真实曲线/亮度转不透明度/渐变映射/多级滤镜页面、浮雕去色修复、渲染/撤销性能优化、双指撤销/三指重做手势（默认启用不受笔模式影响）、长按拾色（5 段灵敏度默认 3）、操作 toast
- **native 构建流程**：`scripts/build_native.sh`（两次 assembleDebug -PbuildNative，先 CMake 编译 jni 再补 jniLibsNativeEmpty 缺失库）；环境 QT_ANDROID_DIR=/opt/Qt6/6.6.3/android_arm64_v8a + KRITA_SRC_DIR=~/Projects/krita-source；增量重编仅 45s；**纯 Kotlin 改动直接 assembleDebug 即可**（预编译 jni 模式）
- **待办**：图像增强其余滤镜（用户中断前保留原色变灰度问题未解决，说拆分后自愈过）；新功能未经真机验证（手机需解锁 adb 安装）

### 当前状态 (2026-08-12 22:00)

- 图层组树形显示:displayRows 递归构建(兄弟逆序,组块保持,嵌套组支持),折叠过滤
- 手势:行内单个 awaitEachGesture(awaitLongPressOrCancellation + swipe/tap 分支),带 LayerPanel Log.d 日志(adb logcat -s LayerPanel 可诊断)
- 已知待验证:长按拖拽/左滑仍可能受 awaitLongPressOrCancellation 严格 slop 影响(400ms 内微动即取消),用户测试后从 logcat 确认
- 二级页面:本地 selectedIndex state(不依赖异步 JNI currentLayerIndex)
- 图标:全部 Tabler 多 path 完整提取;ic_flip_h/ic_flip_v/ic_merge_down 手绘;ic_check 新增
- 不透明度滑块:ReSlider 高度 20dp 胶囊 + 拖动数值 Popup;详情页 50ms 节流
- C++ moveLayer/moveLayerToGroup:KisNodeFacade::moveNode,背景/锁定保护,防组入自身
- 行下方 sub-info:opacity<100% 或 blend≠normal 时显示 "N% · 模式名"(仿 FolioLayers)
- 组二级页面:组显示专属操作(重命名/删除组),不显示图层操作
- 手机当前锁屏:adb 模拟测试受阻,等待用户解锁后测试

### 本轮 commit

- 88e9a4d fix: 图层组树形显示, 手势统一处理, 二级页面本地选中

**2026-08-12 v1.2.1 发布（提交 929658a, tag v1.2.1, Latest）**：修复 Krita 5.x（Qt5）图层树点击即崩溃——`QMouseEvent.position()` 是 Qt6 API，Qt5 只有 `pos()`，Windows 端 Krita 5 用户每次点击图层树都 AttributeError（崩溃点 mousePressEvent 里无条件 `_pen_log` 且在 super 调用前）。修复：① qt_compat.py 新增 `mouse_x`/`mouse_point` 兼容助手（hasattr 自动适配，同 SimpleHSVSliders v1.0.1 惯例）② docker.py 四处未守卫的 `event.position().toPoint()`（hover 日志/tree mousePress 日志/CUSTOM-DROP 日志/_finish_pen_drop）改走 mouse_point ③ `_make_mouse_event` 版本检测 `hasattr(QEvent,'Type')`→`hasattr(QMouseEvent,'position')`（QEvent.Type 在 PyQt5 同样存在，原判断 Qt5 下会误用六参构造器 TypeError——潜在第二崩溃点）④ `_event_is_pen_synth` 补 Qt5 的 QMouseEvent.MouseEventSource 回退。验证：py_compile + offscreen PyQt6 冒烟（真实 QMouseEvent 走 mouse_point 返回正确 QPoint、构造器分支、pen_synth 不抛）。另确认图层行左侧 select_btn 逻辑无 bug：ExtendedSelection 下 setSelected(True) 是增量选择不清其他项（PyQt6 实测 ['A','C']+B→['A','C','B']），行为特征=加选后 activeNode 同步为行序第一选中项（与插件内 Ctrl+Click 一致）。打包流程同 v1.2.0（~/tmp/folio-release-121/，19 文件含 4 native 库+desktop，排除 pycache/qml/qml_list.py/docker_test.py），下载回验 sha256 44d6604c... 一致。注意：v1.2.0 tag 之前只在 GitHub（本地缺失），发布前 git fetch origin --tags 补齐
**2026-08-12 v1.2.1 重新发布（替换旧资产，tag 移至 947e8b2）**：用户（栗原白芷）实测反馈 PgUp/PgDn 切换当前图层时图层树不自动滚动。根因：600ms 轮询同步（_sync_with_krita→refresh_tree）只更新选中态不滚动，_sync_node_tree 的 setCurrentItem 无 ScrollHint 重载，Krita 真实环境下（刷新期间 item 重建+setUpdatesEnabled(False)）不跟随（offscreen 实测 setCurrentItem 默认会滚，Krita 环境差异无法复现）。修复（947e8b2）：refresh_tree 末尾新增_follow_active_layer_if_changed——仅 activeNode 变化时 scrollToItem(EnsureVisible)，避免轮询拉回用户手动浏览位置；_find_tree_item_by_uid 递归查找（3 层嵌套 offscreen 验证）；多选/用户滚动 0.5s 内/刷新中跳过；顺带加固 except 分支 Krita=None + itemWidget isinstance 收窄。用户要求"直接替换 1.2.1"不另起版本号：git tag -f v1.2.1 947e8b2（需 -m 否则触发 vi 编辑器报错）+ push :refs/tags/v1.2.1 删除远程 tag 再 push 新 tag + gh release delete --yes + 重新打包 + gh release create（同 URL）+ 下载回验 sha256 2747fdab... 一致。教训：git tag -f 重打注释 tag 必须带 -m/-F
**2026-08-13 v1.2.1 再次替换（tag 移至 7b41be1）**：Windows 用户（MSN）实测数位笔事件崩溃 AttributeError 'QPoint' object has no attribute 'toPoint'（docker.py_tablet_in_viewport L343）。根因：event.globalPos()（Qt5）直接返回 QPoint，代码统一 .toPoint()（QPointF 方法）——v1.2.1 只修了局部坐标 position，**全局坐标遗漏**。修复（7b41be1）：qt_compat 新增 mouse_global_point（Qt6 globalPosition()->QPointF 转 QPoint，Qt5 globalPos() 直接用），docker.py 两处（_handle_tablet_as_mouse L303、_tablet_in_viewport L341）改用助手去掉 .toPoint()；PyQt5 fallback 导入/krita 条件导入/FlowLayout 覆写加 type: ignore 标注（消除环境性静态误报，pi-lens 此后 Python clean）。offscreen 验证 Qt6 真实事件+Qt5 fallback 逻辑全过。**网络教训**：FlClash fake-ip 导致 git/gh TLS eof（间歇性）——curl 直连真实 IP 可通（github.com=20.205.243.166, api.github.com=20.205.243.168, uploads.github.com=20.205.243.161）；git 用 `-c http.curloptResolve=host:443:IP -c http.version=HTTP/1.1`（HTTP/1.1+重试循环成功率大增）；gh release 操作改用 curl --resolve 直连 API（删 release→POST releases→PATCH uploads assets），gh release delete 曾报 EOF 但实际生效需二次确认。asset 上传经 uploads.github.com 直连，下载回验 sha256 67c30e55... 一致
**2026-08-14 Antigravity 桌面应用与 CLI**：桌面应用 AUR 包 antigravity 已更新至 2.8.1-1（本地修改 PKGBUILD 构建安装，AUR 源仍停留 2.6.0-1 且被标记"过期"；官方最新版从 <https://antigravity.google/download> 确认，下载 URL 形如 <https://storage.googleapis.com/antigravity-public/antigravity-hub/><ver>-<build>/linux-x64/Antigravity.tar.gz，本地 PKGBUILD 副本在 ~/tmp/antigravity-pkgbuild/，含新 _build=6512087774658560 和 b2sum）。更新流程：paru 只能拿到 2.6.0 → 需手动 git clone AUR 仓库改 pkgver/_build → updpkgsums 重算校验和 → makepkg -f → paru -U 安装（sudo 需交互终端）。**CLI（agy）独立安装**：`~/.local/bin/agy` 是 Go 编译的独立 ELF（约 206MB，另有 .old 备份和 agy-wrap.sh 包装脚本——unset SSH_* 并设置 DBUS_SESSION_BUS_ADDRESS/XDG_RUNTIME_DIR 后 exec agy，供桌面环境启动用），版本 1.1.13 已是最新（`agy update` 自管理，不随 pacman 更新）。`antigravity --version` 输出的是 Electron 内置 node 版本（v24.x）而非应用版本，别混淆
**2026-08-21 QQ 踢线复发根因修复（HMA 配置缺口）**：QQ 9.3.30 再次无提示掉线，排查确认 Xposed 模块（ntqqbattery/qqhook/qfun）确已全禁用、Thanox 无 QQ 规则、Vector Framework (zygisk_vector v2.2) 无启用模块 scope 含 mobileqq；真正根因是 HMA-OSS (`org.frknkrc44.hma_oss`, configVersion 93) 的 Root-Hide scope 列表 16 个应用唯独漏了 `com.tencent.mobileqq`——内核无 SUSFS，防检测全靠 HMA，QQ 裸奔被 QSec 检出 KernelSU/Zygisk 痕迹；修复：照抄 com.tencent.mm 条目（aggressiveFilter=false, useWhitelist=false, excludeSystemApps=false, applyTemplates=["Root-Hide"]）加入 scope，经 /data/local/tmp 中转 cp 回 `/data/user/0/org.frknkrc44.hma_oss/files/config.json` 并 chown u0_a518 + chmod 600 + restorecon，已强停 QQ 待用户重登验证；若仍踢线后备方案为加装 Zygisk-Assistant 类自隐藏模块
